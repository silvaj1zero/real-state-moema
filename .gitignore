

# Environment & Secrets (AIOX)
.env
.env.local
.env.*.local
*.key
*.pem

# Dependencies (AIOX)
node_modules/
node_modules

# Build & Logs (AIOX)
dist/
build/
*.log
logs/

# IDE & OS (AIOX)
.DS_Store
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt

# AIOX Runtime (gitignored)
.aiox/handoffs/
.aiox/cache/
.aiox/sessions/

# Pro License (large binary manifests)
pro-version.json
pro-installed-manifest.yaml

# Framework Core (installed dependency, not source)
.aiox-core/

# IDE Agent Configs (per-user)
.codex/
.cursor/
.gemini/
.antigravity/
.agent/

# Squads (installed dependency)
squads/

# Claude memory (per-user)
.claude/hooks/

# Backup files (prevent credential leaks)
*.backup.*
*.backup
.env.backup*
