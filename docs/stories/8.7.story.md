# Story 8.7: Polish dos entregáveis ACM — branding oficial, cabeçalho e integridade de dados

**Epic:** 8 — Geração de Entregáveis ACM
**Camada:** Renderização + Dados
**Status:** InReview
**Priority:** Should
**Estimated Complexity:** S (3-5 pts)

> **Origem:** feedback da founder/consultora após revisão dos PDFs de preview (2026-06-16): (a) ajustar o cabeçalho de todos os PDFs e usar os logos oficiais RE/MAX; (b) garantir que dorms/suítes/vagas e links de anúncio sejam **dado confirmado, nunca inventado** (Art. IV); (c) surfaçar links de anúncio para revisão humana; (d) preparar plotagem de comparáveis no mapa.

## Acceptance Criteria

- [x] AC1: **Logos oficiais RE/MAX** embutidos nos 4 documentos (balão + wordmark) via `brandAssets.ts` (base64 data URL — self-contained, funciona no browser e em node/testes). Substitui o lockup tipográfico.
- [x] AC2: **Cabeçalho redesenhado** em todos os PDFs: wordmark + "GALERIA · MOEMA" + consultora (nome/CRECI/telefone/e-mail reais do deck v2) + régua vermelha RE/MAX. Deck usa balão em slides escuros (capa/encerramento) e wordmark nos claros.
- [x] AC3: **Integridade de dados (Art. IV):** dorms/suítes/vagas exibidos SOMENTE quando vêm do DB (`acm_comparaveis`); ausente = "—", nunca inventado. (Auditoria: o código de produção já degradava para "—"; o preview anterior fabricava S/V/D — corrigido.)
- [x] AC4: **Links de anúncio para revisão humana:** campo `anuncioUrl` real no `LaudoSourceComparable`, surfaçado como link clicável (`<Link>`) no Laudo Sec. 7.1 e Didático Parte 4. Sem link → "sem link público / não recuperável" (nunca inventado).
- [x] AC5: **Preparo do mapa:** campos `lat`/`lng` opcionais no source + migration `20260616000001_acm_rpc_coords_anuncio_url.sql` (extende a RPC com latitude/longitude + anuncio_url via LEFT JOIN em `scraped_listings`) — **staged para @data-engineer** aplicar e regenerar tipos.
- [x] AC7: **BUG DE PRODUÇÃO encontrado + corrigido na migration:** a RPC viva `fn_comparaveis_no_raio` referenciava `ac.ano_referencia` (coluna inexistente — a tabela tem `data_referencia`), quebrando a tela ACM com `column ac.ano_referencia does not exist`. A migration agora mapeia `ano_referencia := EXTRACT(YEAR FROM ac.data_referencia)`. **PENDENTE:** aplicar via Supabase SQL Editor (`DATABASE_URL` local está com senha obsoleta — 28P01; runtime usa PostgREST, não afetado). **A tela ACM não funciona até aplicar.**
- [x] AC6: 117/117 testes ACM · 0 tsc · 0 lint. Logos base64 renderizam em node (`renderToBuffer`).

## Out of Scope / Pendente

- **Aplicar a migration do RPC** (DDL → @data-engineer/@devops, via SQL Editor) + regenerar tipos TS + mapear `anuncioUrl`/`lat`/`lng` nos export sheets (hoje degradam para null). **Depois disso**: links de anúncio reais e pins de comparáveis no mapa passam a aparecer automaticamente.
- ZIP do pacote + anexar ao dossiê (fast-follow da 8.6).

## File List

**Novos:**
- `app/src/lib/acm/pdf/brandAssets.ts` — logos RE/MAX (balão + wordmark) em base64.
- `supabase/migrations/20260616000001_acm_rpc_coords_anuncio_url.sql` — RPC com lat/lng + anuncio_url (staged).

**Alterados:**
- `app/src/lib/acm/pdf/theme.ts` — contato real da consultora + re-export dos assets.
- `app/src/lib/acm/pdf/{ResumoDocument,LaudoDocument,DeckDocument,DidaticoDocument}.tsx` — cabeçalho com logo + régua vermelha; Laudo/Didático com link de anúncio (`<Link>`).
- `app/src/lib/acm/pdf/laudoModel.ts` — `anuncioUrl`/`lat`/`lng` no source; `anuncioUrl` na rastreabilidade; status "não recuperável" honesto.
- `app/src/lib/acm/pdf/didaticoModel.ts` — `anuncioUrl` na validação Top N.

## Change Log

| Data | Agente | Mudança |
|---|---|---|
| 2026-06-16 | @dev (Dex) | Branding oficial + cabeçalho redesenhado + integridade de dados (links reais, sem invenção) + migration do RPC para coords/anuncio_url. 117/117 testes · 0 tsc · 0 lint. Status → InReview. |
