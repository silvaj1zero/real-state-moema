# Story 9.4: Riqueza de dado — fechar AC3 do sink ITBI (área constr×terreno, SQL, S/V/D, padrão, ano, valor venal)

**Epic:** 9 — ACM: Paridade de Entrega Premium + Suporte a Apartamento
**Camada:** Dados (cross-repo)
**Status:** Done
**Priority:** Must
**Estimated Complexity:** M (5-8 pts)
**Executor sugerido:** @data-engineer + @devops (coordenação cross-repo)

> **Cross-repo:** o sink vive em `acm-imobiliario` (`engine/src/sinks/supabase_acm.py`), **fora deste repositório**. Esta story rastreia e coordena a mudança; o código-fonte é alterado no repo do engine, mas o **resultado** (colunas preenchidas) é validado contra o banco PROD deste projeto. É o que separa "mesma forma" de "mesma evidência" na comparação.

## User Story

Como Luciana (consultora),
Quero que as vendas ITBI carregadas tragam **área construída e de terreno separadas, SQL cadastral, dormitórios/suítes/vagas, padrão, ano e valor venal** (e não tudo colapsado em `area_m2`/`notas`),
Para que o Score seja calculado com confiança, o Top 5 tenha rastreabilidade SQL/ITBI e a aba Terrenos da planilha exista — igualando a evidência do laudo de referência.

## Contexto

A comparação mostrou que o laudo/planilha real exibe S/V/D, SQL cadastral e separação terreno×construção por comparável; no Moema esses campos estão **NULL** nas 3.618 linhas ITBI porque `montar_registros` no sink colapsa em `area_m2` e joga o SQL em `notas`. A migration que cria as colunas já foi aplicada (`20260615000002`); falta o **mapeamento no sink** + **backfill** das linhas existentes. A magnitude exata dos NULLs é medida na 9.0.

## Acceptance Criteria

- [x] AC1: **Mapeamento no sink** (`montar_registros`, repo `acm-imobiliario`): popular `area_construida_m2` e `area_terreno_m2` separados (não colapsar em `area_m2`), `sql_cadastral` (do `id_fonte`), `padrao_iptu`, `ano_construcao`/`ano_referencia`, `testada_m`, `valor_venal`, `tipo`. `dormitorios/suites/vagas` quando houver anúncio casado (NULL p/ ITBI puro). _(12-Jul: `_campos_metodologia` no sink + ampliação R5; engine local `squads-custom/acm-imobiliario/`, backup prévio.)_
- [x] AC2: **Distinção apto×casa** propagada quando a fonte (uso/descrição IPTU) permitir — habilita `propertyType` da 9.1 a partir do dado real. _(12-Jul: `uso_iptu`/`complemento`/`padrao_iptu` em PROD; smoke `9.4-inapp-smoke.mjs` — gate R5 alvo casa exclui 780 verticais VIA GUIA, incl. Andrade Pertence 45 "AP 82".)_
- [x] AC3: **Backfill** das 3.618 linhas ITBI de Moema já em PROD (reprocessar/atualizar), de forma idempotente e auditável (contagem antes/depois por coluna). _(12-Jul: `backfill_story94.py` — UPDATE por id, match 3.618/3.618, 0 erros; relatório antes/depois em `engine/data/output/backfill_story94_report.json`.)_
- [x] AC4: **Validação no banco deste projeto:** após o backfill, a cobertura NULL medida na 9.0 cai para a meta definida (ex.: `sql_cadastral` e áreas separadas ~100% nas linhas ITBI; S/V/D conforme disponibilidade de anúncio). Tabela antes/depois anexada. _(12-Jul: `9.4-sink-coverage.mjs` EXIT 0 — 0% NULL em todos os campos-meta; `complemento` 8,5% best-effort; S/V/D 100% NULL = ITBI puro, sem meta. Tabela em `docs/acm/9.4-sink-ac3-verification.md` §5.)_
- [x] AC5: **RPC inalterada ou compatível:** `fn_comparaveis_no_raio` continua retornando as colunas esperadas pela 8.2/9.2/9.3; nenhum contrato quebrado. _(11-Jul: RPC estendida ADITIVAMENTE p/ 32 colunas — contrato 8.1/8.7 verbatim; smoke PROD 193 comparáveis + suíte ACM 299 tests PASS.)_
- [x] AC6: **Sem regressão de dado:** linhas não-ITBI (manual/scraping) intactas; nenhuma perda de registro. _(12-Jul: backfill é UPDATE por id restrito a `fonte='itbi'` + notas `[ITBI]%` do consultor — zero delete/insert; suíte ACM 27 files / 303 tests PASS, datasets congelados inalterados.)_

## Dependencies

- Depends on: 9.0 (mede o gap real e define metas de cobertura).
- Habilita: 9.1 (régua de apto com dado real), 9.2 (aba Terrenos + colunas SQL/S/V/D), 9.3 (não diretamente, mas melhora rótulos), 9.5 (status web casa com SQL/anúncio).
- Cross-repo: `acm-imobiliario` — operações de push/release coordenadas por @devops.

## Ampliação de escopo (09-Jul-2026 — incidente tipologia R5)

Além dos campos AC1 originais, o sink/backfill **deve** passar a ingerir (quando existirem na guia):

- **Complemento** (ex.: `AP 82`) — causa-raiz do incidente casa×apto em Andrade Pertence  
- **Uso (IPTU)** / descrição de uso e padrão finos  
- **Fração ideal, testada, ACC** (além de áreas já citadas)

Isso habilita a **Story 9.17 (R5 industrializado)**. Sem Complemento/Uso, R5 permanece crosscheck offline com arquivos da SF.

## Out of Scope

- Render de planilha/laudo/mapa → 9.1/9.2/9.3.
- Geocodificação/ingestão nova de ITBI → já coberta pelo engine/Epic 7.
- Validação web (Fase B) → 9.5.
- Gate de filtro no compute/PDF → 9.17.

## Technical Notes

- **Fronteira de autoria:** o `.py` é editado no repo do engine (não neste). Aqui ficam o **rastreamento**, os **critérios de aceite** e a **verificação contra o banco**. @devops faz a ponte de push/release no repo do engine.
- **AC0 (Story 8.1) já confirmou disponibilidade** dos campos na fonte ITBI/IPTU — esta story operacionaliza o mapeamento + backfill.
- Backfill deve ser **idempotente** (re-rodar não duplica) e medir contagem por coluna antes/depois.
- LGPD: ITBI = endereço + valor; manter privado/local-only.

## File List

_Predominantemente no repo `acm-imobiliario` (fora deste repo). Neste repo:_
- `docs/acm/9.4-sink-ac3-verification.md` — contrato de campos + metas + checklist handoff + resultado §5
- `app/scripts/acm-audit/9.4-sink-coverage.mjs` — cobertura NULL vs metas (READ-ONLY PROD)
- `app/scripts/acm-audit/9.4-inapp-smoke.mjs` — smoke de aceite (a)/(b): RPC → adapter → computeLaudo real (12-Jul)
- `app/src/lib/acm/adapter.ts` — mapeia `data_venda`→`dataVenda` (competência), R5 (`uso_iptu`/`padrao_iptu`/`complemento`/`sql_cadastral`) e `bairro_real` (12-Jul)
- `app/src/lib/acm/adapter.test.ts` — +4 tests do mapeamento 9.4
- `app/src/lib/supabase/types.ts` — tipos opt-in `complemento`/`uso_iptu`/`fracao_ideal` + `bairro_real`/`data_venda`/AC1 na `ComparavelNoRaio`
- `supabase/migrations/20260711000001_story94_r5_ingest_columns.sql` — colunas R5 + `bairro_real` + `padrao_iptu`→TEXT (**aplicada PROD 11-Jul**)
- `supabase/migrations/20260711000002_story94_rpc_r5_data_venda.sql` — RPC 32 colunas incl. `data_venda` (**aplicada PROD 11-Jul**, trap 42P13)

_No engine local `workspace/businesses/luciana-borba/squads-custom/acm-imobiliario/engine/` (pasta gitignorada, sem GitHub; backup `_backups/acm-imobiliario-backup-2026-07-12.zip`):_
- `src/schema.py` — colunas canônicas `complemento`/`uso_iptu`/`padrao_iptu`/`fracao_ideal`/`cep`
- `src/sources/pmsp_itbi.py` — `para_schema` preserva os campos da guia (antes descartados)
- `src/viacep.py` (novo) — CEP→`bairro_real` com cache + aliases de mercado (= `geoConfig`)
- `src/sinks/supabase_acm.py` — `_campos_metodologia` + mapeamento ampliado no `montar_registros`
- `push_acm_supabase.py` — enriquece `bairro_real` antes do sink
- `backfill_story94.py` (novo) — backfill idempotente (UPDATE por id) + auditoria antes/depois

## Change Log

| Data | Agente | Mudança |
|---|---|---|
| 2026-06-17 | @sm (River) | Story criada a partir do Epic 9 (caminho crítico). Status Draft. Mantida como Must com nota cross-repo (decisão founder 2026-06-17). |
| 2026-06-17 | @po (Pax) | `*validate-story-draft`: GO (8/10). Aceite neste repo = verificação de cobertura no banco (AC4); push/release do engine via @devops. Status Draft → **Ready**. |
| 2026-07-09 | @dev | Contrato + script de cobertura + tipos R5 neste repo. Sink `.py` permanece no engine. |
| 2026-07-11 | @data-engineer | **Sessão B (lado-app) executada:** migrations `20260711000001`/`20260711000002` aplicadas em PROD via Management API (colunas R5 + `bairro_real` + `padrao_iptu`→TEXT; RPC 32 cols com `data_venda`). AC5 verificado (smoke 193 comparáveis; tsc/eslint/vitest 299 PASS). Cobertura re-medida: colunas existem, 100% NULL — AC1/AC3/AC4 aguardam sink/backfill no engine (sessão C, bloqueada na localização do repo). |
| 2026-07-12 | @data-engineer + @dev | **Sessão C (engine) executada — story fechada.** Backup do engine (`_backups/acm-imobiliario-backup-2026-07-12.zip`, 96,8 MB). Sink ampliado (schema + `para_schema` + `_campos_metodologia` + ViaCEP) e `backfill_story94.py`: dry-run match 3.618/3.618 → apply **3.618 UPDATE, 0 erros** (idempotente, ids preservados). Aceite: `9.4-sink-coverage.mjs` **EXIT 0** (0% NULL em todos os campos-meta; complemento 8,5% best-effort); adapter do app mapeia `data_venda`/R5/`bairro_real` (+4 tests); smoke `9.4-inapp-smoke.mjs`: homogeneização **ajustados=68/semAjuste=0**, gate R5 **780 verticais excluídos via guia**. tsc/eslint exit 0; suíte ACM **303 PASS**; flake8 engine exit 0. Fechamento autorizado pelo founder na sessão. Status → **Done**. |
