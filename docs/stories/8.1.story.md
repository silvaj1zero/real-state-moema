# Story 8.1: Modelo de dados da metodologia ACM (construído × terreno, Score, SQL cadastral, ask-vs-close)

**Epic:** 8 — Geração de Entregáveis ACM
**Camada:** Dados
**Status:** Done (código completo; aplicar migration RPC `20260615000004` via SQL Editor)
**Priority:** Must
**Estimated Complexity:** M (5-7 pts)

> **PO Validation (2026-06-15, Pax):** GO condicional (7/10). Fix obrigatório: AC0 (spike de disponibilidade de dados ITBI).
> **AC0 resolvido (2026-06-15, Aria):** inspeção do engine (`engine/src/schema.py`, `engine/src/sinks/supabase_acm.py`) confirmou que a fonte **já traz todos os campos** (área construída×terreno, testada, ano_construcao, **padrão IPTU 0-5**, valor_venal, SQL `id_fonte`; dorms/suítes/vagas só de anúncios). O gap é o **sink descartar** dado (colapsa em `area_m2`, SQL em `notas`). Migration draft real: `supabase/migrations/20260615000002_acm_methodology_fields.sql`. **Mudança de sink necessária** (repo externo) documentada abaixo.

## AC0 — Achados do spike (disponibilidade fonte → coluna)

| Campo | Fonte | Engine (`schema.py`) | Ação |
|---|---|---|---|
| `area_construida_m2` / `area_terreno_m2` | ITBI/IPTU | ✅ ambos | colunas novas (hoje colapsados em `area_m2`) |
| `testada_m`, `ano_construcao`, `padrao` (0-5), `valor_venal` | ITBI/IPTU | ✅ | colunas novas; `padrao` → input oficial de Score (Roadmap 2.2) |
| `id_fonte` (SQL cadastral) | ITBI | ✅ (vai em `notas`) | promover a `sql_cadastral` |
| `dormitorios/suites/vagas` | só anúncios | ✅ quando há anúncio | colunas nullable |
| pedido/deságio | match anúncio | ✅ quando recuperado | `preco_pedido`/`desagio_percent`/`status_anuncio` |

**Mudança de sink (repo `acm-imobiliario`, fora deste repo — @devops/owner do engine):** `montar_registros` em `engine/src/sinks/supabase_acm.py` deve mapear os campos novos em vez de colapsar (`area_construida_m2`/`area_terreno_m2` separados; `id_fonte` → `sql_cadastral`; `padrao`, `ano_construcao`, `testada_m`, `valor_venal`, `tipo`). Sem isso, as colunas novas ficam NULL mesmo com a migration aplicada.

## User Story

Como sistema ACM,
Quero modelar os atributos que a metodologia da Luciana usa (área construída × área de terreno, programa, Score, SQL/ITBI, preço pedido vs. fechado),
Para que os cálculos do laudo (efeito-escala do terreno, índice de aderência, deságio medido) tenham insumo real, em vez do único `area_m2` atual.

## Contexto

`acm_comparaveis` hoje (ver `app/src/lib/supabase/types.ts` L254) tem um único `area_m2`, `preco`, `preco_m2`, `is_venda_real`, `fonte`, `coordinates`. O laudo Honduras distingue fortemente **área construída** e **área de terreno**, R$/m² de cada, dorms/suítes/vagas, **Score B**, **SQL** (Setor/Quadra/Lote do GeoSampa) e **preço pedido vs. fechado** (deságio -10% a -15%). Esses campos não existem.

## Acceptance Criteria

- [x] **AC0 (spike — bloqueia AC1):** Confirmar **quais campos a fonte ITBI realmente fornece** antes de criar colunas. Inspecionar `engine/src/sinks/supabase_acm.py` e `engine/src/sources/pmsp_itbi.py` (repo `acm-imobiliario`) + a amostra do dry-run: existem **área de terreno, SQL cadastral (Setor/Quadra/Lote) e ano** por transação? Registrar o mapeamento real fonte→coluna. Se algum campo não vier da fonte, marcá-lo como `NULL`/derivado e ajustar 8.2. **Saída:** tabela de disponibilidade de campos anexada à story.
- [x] AC1: Migration nova (idempotente, `IF NOT EXISTS`) adiciona a `acm_comparaveis` (apenas campos confirmados disponíveis em AC0):
  - `area_terreno_m2 NUMERIC`
  - **Migração de `area_m2`:** adicionar `area_construida_m2 NUMERIC`; backfill `area_construida_m2 = area_m2` numa migration; manter `area_m2` como coluna legada por 1 release (janela de depreciação), com os consumidores (`useAcm`, `AcmTable`, RPC) migrados para `area_construida_m2` na Story 8.2; remoção de `area_m2` fica como tech-debt pós-8.2.
  - `preco_m2_terreno NUMERIC` (gerado/derivado: `preco / area_terreno_m2`)
  - `dormitorios SMALLINT`, `suites SMALLINT`, `vagas SMALLINT` (nullable)
  - `score TEXT` CHECK IN ('AAA','AA','A','B') — nullable (calculado em 8.2, persistido opcional)
  - `sql_cadastral TEXT` (Setor/Quadra/Lote PMSP; nullable)
  - `ano_referencia SMALLINT` (ano da transação ITBI; nullable)
  - `preco_pedido NUMERIC` (asking, quando anúncio recuperado; nullable) + `desagio_percent NUMERIC` (derivado quando há `preco_pedido` e venda)
  - `status_anuncio TEXT` CHECK IN ('confirmado','parcial','off_market','nao_recuperavel') — nullable
- [x] AC2: A RPC `fn_comparaveis_no_raio` retorna os novos campos (SQL da função: migration `20260615000004` — `CREATE OR REPLACE` fiel à versão viva, só anexa colunas; tipo `ComparavelNoRaio` em `types.ts` estendido). **Aplicar via SQL Editor.**
- [~] AC3: O sink ITBI do engine (`supabase_acm.py`) mapeia os campos disponíveis — **cross-repo (referência, não escopo de código deste repo)**. Documentado no spike AC0.
- [x] AC4: Backfill seguro: linhas existentes ficam com campos novos `NULL` (migration `IF NOT EXISTS` + colunas nullable; tipos opcionais → `useAcm`/`AcmExportMenu` não quebram).
- [x] AC5: `AddComparableSheet.tsx` e `useCreateComparavel` ganham os campos novos (opcionais) no formulário manual.
- [~] AC6: Tipos regenerados (`supabase gen types typescript`) — **Deferred** para `@devops` após apply (mesmo padrão da Story 7.1 AC5).
- [x] AC7: Testes: insert com campos novos + derivados (`useAcm.test.ts`); CSV de export inclui as colunas novas sem quebrar o cabeçalho atual (`AcmExportMenu.test.ts`). RPC-retorna-colunas é validado pela migration + tipo (DB-side).

## Dependencies

- Depends on: integração ITBI (`20260615000001_add_itbi_fonte_comparavel.sql` aplicada + `--apply` do engine), Story 3.x (módulo ACM base).
- Blocks: 8.2 (cálculos consomem estes campos), 8.3 (laudo lê estes campos).

## Out of Scope

- Cálculo do Score e aderência (Story 8.2) — aqui só a coluna persiste o resultado.
- Geração de PDF (8.3/8.4).
- Campos de regressão hedônica / NBR (épico futuro — Roadmap Fase 2).

## Technical Notes

- **Migration sempre idempotente** (`ADD COLUMN IF NOT EXISTS`).
- **Caveat scaffold Supabase:** as migrations reais vivem em `supabase/migrations/`; o `config.toml`/link do CLI está aninhado em `supabase/supabase/` e há um scaffold perdido em `C:\Users\Zero\supabase` que captura o workdir do CLI — `db push` exige `--workdir` explícito e está dessincronizado (precisa `migration repair`). Ver handoff. **Aplicar via `--workdir` + repair, ou SQL direto idempotente.**
- Preferir `area_construida_m2` / `area_terreno_m2` como nomes canônicos; depreciar `area_m2`.

## File List

**Implementado nesta sessão (2026-06-15):**
- `app/src/lib/supabase/types.ts` — `AcmComparavel` + `ComparavelNoRaio` ganham os campos da metodologia (nullable/opcionais); novos tipos `AcmScore`, `AcmStatusAnuncio` (AC2-tipos, AC4).
- `app/src/hooks/useAcm.ts` — `CreateComparavelInput` + insert do `useCreateComparavel` persistem os campos novos + derivados (`preco_m2_terreno`, `desagio_percent`) (AC5).
- `app/src/components/acm/AddComparableSheet.tsx` — seção "Detalhes da metodologia (opcional)": área terreno, preço pedido, dorms/suítes/vagas, SQL cadastral, ano referência; relabel "Área m²"→"Área construída m²" (AC5).
- `app/src/hooks/useAcm.test.ts` — testes do insert com campos novos + derivados + ausência→NULL (AC7-parcial).
- `supabase/migrations/20260615000002_acm_methodology_fields.sql` — migration AC1 (já existente do spike, aplicada via SQL Editor).

**Desbloqueado (def viva fornecida pelo founder via SQL Editor):**
- `supabase/migrations/20260615000004_acm_rpc_comparaveis_methodology_fields.sql` — `CREATE OR REPLACE fn_comparaveis_no_raio` **fiel** à versão viva (assinatura lat/lng, ST_DWithin, ORDER BY preservados verbatim); só anexa as colunas novas ao `RETURNS TABLE` e ao `SELECT` (AC2-SQL). **Aplicar via SQL Editor.**
- `app/src/components/acm/AcmExportMenu.tsx` — `buildComparaveisCsv` (pura, exportada) anexa as colunas da metodologia ao CSV sem quebrar o cabeçalho (AC7).
- `app/src/components/acm/AcmExportMenu.test.ts` — testes do CSV (AC7).

## Change Log

| Data | Agente | Mudança |
|------|--------|---------|
| 2026-06-15 | @po (Pax) | GO condicional 7/10 → AC0 (spike) obrigatório. |
| 2026-06-15 | @architect (Aria) | AC0 resolvido (inspeção do engine); migration draft real criada. Status →Ready. |
| 2026-06-15 | @dev (Dex) | AC2-tipos + AC4 + AC5 + AC7-parcial implementados. Validação: 51/51 testes ACM, 0 erros tsc em `src/`, lint exit 0. **AC2-SQL bloqueado** na def viva da RPC. Status →InReview. |
| 2026-06-15 | @dev (Dex) | **Desbloqueado:** founder forneceu a def viva via SQL Editor. Migration RPC `20260615000004` (CREATE OR REPLACE fiel) + CSV export `buildComparaveisCsv` + testes. Validação: 56/56 testes ACM, 0 erros tsc em `src/`, lint exit 0. |
| 2026-06-15 | @qa (Quinn) | QA Gate: **PASS** (obs: aplicar migration RPC via SQL Editor; AC3 sink engine cross-repo; AC6 regen tipos @devops). Status →Done. |
