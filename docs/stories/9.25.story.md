# Story 9.25 — Tribunal: Teste de Robustez da Tese (leave-one-out + admissibilidade das testemunhas)

**Epic:** 9 — ACM Paridade Premium
**Status:** Ready
**Priority:** Should (Fase 2 do veredito ROI — "altíssimo valor em V2", defesa sob contraditório)
**Executor sugerido:** @dev · **Routing:** Opus (metodologia estatística + narrativa técnica)
**Estimativa:** M (1–1,5 dia)
**Origem:** `docs/acm/VEREDITO-ROI-UNICO-20260709.md` §2 ("ACM como tribunal") — era o "9.23 a criar" do veredito v3; **renumerada para 9.25** (Wave 4 ocupou o número; ver ROADMAP §9c).

## Story

Como consultora, quero uma seção "Teste de Robustez da Tese" no laudo V2 técnico que prove que o número NÃO depende de um comparável isolado (leave-one-out) e apresente cada comparável como testemunha com admissibilidade A/B/C, para defender o laudo sob contraditório (proprietário cético, corretor concorrente, perícia).

## Contexto

Pré-requisitos da anti-lista do veredito ("PDF criativo sem passaporte no JSON = teatro") estão satisfeitos: passaportes A/B/C por comparável (9.15, `ComparavelPassport` em `methodology.ts`), mediana ponderada com evidência A/B (9.20, `EvidenciaRanking`), headline em faixa (9.10). Falta a mecânica de robustez: quanto a referência se move se retirarmos cada testemunha.

## Acceptance Criteria

1. **AC1 — Função pura `testarRobustez(computation)`** em `app/src/lib/acm/robustezTese.ts`:
   - **leave-one-out** sobre o conjunto do cenário aderente de referência (Top 5 ou Top 3, o mesmo do headline): re-mediana sem cada item → `amplitudeLeaveOneOutPct` (máx. desvio relativo vs referência) e `comparavelMaisInfluente`;
   - `veredicto: 'robusta' | 'sensivel'` com limiar PARAMETRIZADO (`opts.limiarPct`, default declarado no código com justificativa — sugerido 10%, marcado como parâmetro de arbítrio, NÃO verdade estatística);
   - quadro de testemunhas: reaproveita `passaportes` (endereço, nível A/B/C, motivos) — sem recomputar nada.
2. **AC2 — Aviso integrado:** quando `veredicto === 'sensivel'`, adicionar aviso (novo código canônico `reference_sensitive_to_single_comp`, severidade `atencao`) via o mecanismo da 9.15 — no MODEL (não mutar o computation), ou expor o relatório para o model decidir.
3. **AC3 — Exibição SÓ no laudo V2 técnico:** seção nova no `laudoModel.ts`/`LaudoDocument.tsx` (após a sensibilidade, Sec. 9x). NÃO entra no Lite (decisão H-3 D/E: camada técnica é V2). Didático ganha 1 parágrafo explicando o teste (transparência).
4. **AC4 — Determinismo:** função pura, mesma entrada → mesma saída; sem Date/random; leave-one-out documentado com fórmula no JSDoc.
5. **AC5 — Testes:** fixture Honduras (amplitude esperada baixa — amostra n=23 homogênea) + caso sintético com outlier dominante (amplitude alta → `sensivel` + comparável certo apontado) + AP113/AP132 regressão sem drift. Suite verde.

## Tasks

- [ ] `robustezTese.ts` + testes (AC1/AC4/AC5)
- [ ] Código de aviso novo + integração no model (AC2)
- [ ] Seção no laudo V2 + parágrafo no didático (AC3)
- [ ] lint / typecheck / vitest com saída real

## Dependencies

Hard: nenhuma (9.15/9.20 Done). Relação: 9.24 (simulador) — independentes, mesma wave.

## Out of Scope

- Bootstrap/intervalo de confiança formal (NBR Grau III — trilha C-2/N3, "sob demanda" per §10.3).
- Leave-one-out sobre TODOS os cenários (só o de referência do headline).

## Dev Notes (executor externo, contexto zero)

- A mediana de referência vem de `computation.headline.referencia` (cenário top5/top3) — os membros do conjunto estão em `computation.top5`/`top3` (`AdherenceBreakdown[]`). A mediana ponderada A/B/C (9.20) é a base — replicar a MESMA função de mediana usada no compute (exportá-la se privada; não duplicar critério).
- Passaportes: `computation.passaportes` (`ComparavelPassport[]`) já tem nível + motivos — o quadro de testemunhas é formatação, zero lógica nova.
- Gate (de `app/`): `npm run typecheck` · `npx eslint src/lib/acm --max-warnings 0` · `npx vitest run src/lib/acm --no-file-parallelism`.
- Cuidado: NÃO alterar `computeLaudo` de forma que mude números existentes — regressões Honduras/AP são gabaritos congelados (rule gate-determinism).

## File List (esperado)

`app/src/lib/acm/robustezTese.ts` · `robustezTese.test.ts` · `methodology.ts` (só export de helper de mediana, se necessário) · `pdf/laudoModel.ts` · `pdf/LaudoDocument.tsx` · `pdf/didaticoModel.ts` · esta story.

## Change Log

| Data | Agente | Mudança |
|------|--------|---------|
| 2026-07-10 | @sm (draft autônomo Fable) | Criada do veredito ROI §2 "tribunal" (renumeração: ex-"9.23" do veredito v3). Status Draft. |
| 2026-07-10 | @po (validação autônoma) | Checklist 10 pontos: 10/10 GO — pré-requisito anti-lista (passaporte no JSON) verificado Done (9.15/9.20); AC1 limiar parametrizado evita invenção; AC5 inclui caso sintético sensível. Status → Ready. |

## Dev Agent Record

_(preencher na implementação)_

## QA Results

_(preencher no gate)_
