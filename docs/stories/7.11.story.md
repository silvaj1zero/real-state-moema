# Story 7.11: Detecção FISBO determinística via `publisherType` nativo (ZAP/VivaReal)

**Epic:** 7 — Inteligencia de Prospeccao Automatizada Multi-Fonte
**Wave:** A (refino de qualidade)
**Status:** Done
**Priority:** Should (alto ROI — baixo esforço, ganho direto de qualidade de lead)
**Estimated Complexity:** S (3-5 pts)

## User Story

Como sistema de prospeccao,
Quero usar o campo nativo `publisherType` (OWNER/AGENCY/DEVELOPER) dos feeds de ZAP/VivaReal como sinal determinístico,
Para classificar anunciantes (FISBO vs imobiliária vs construtora) com precisão direta, em vez de depender só da heurística 4-signal quando a fonte já entrega o dado.

## Contexto

A pesquisa `docs/research/2026-06-15-fisbo-captacao-ingestao/report.md` (achado F1, confiança alta 3-0) confirmou que os scrapers de ZAP/VivaReal (ex.: Apify `haketa/zapimoveis-scraper`, `jungle_synthesizer/brazil-vivareal-zap-imoveis-scraper`) expõem nativamente `publisherType` ∈ {OWNER, AGENCY, DEVELOPER} + presença de CRECI, em 40+ campos com nome/telefone/email do publisher. ZAP e VivaReal compartilham o backend Glue API da OLX.

Hoje `classifyAdvertiser` (`app/src/lib/scrapers/classify-advertiser.ts`, ADR-EPIC7-004) usa só a heurística 4-signal (CNPJ, CRECI, DDD/telefone móvel, contagem de anúncios) — estimativa 70-85% de precisão. Onde a fonte entrega `publisherType`, temos um sinal **determinístico** que supera a heurística.

## Acceptance Criteria

- [x] AC1: Schema `app/src/lib/schemas/epic7/advertisers.ts` ganha campo opcional `publisher_type` enum `['owner','agency','developer']` (nullable — fontes sem o campo, ex.: MercadoLivre, ficam null). → `PublisherTypeSchema` + `publisher_type: .nullable().optional()` no `AdvertisersSchema`.
- [x] AC2: `classifyAdvertiser` ganha um **passo 0 determinístico** (antes da heurística): se `publisher_type` presente → mapeia direto: `owner→for_sale_by_owner` (confidence 0.95), `agency→broker` (0.95), `developer→builder` (0.95), com novo `ClassificationSignal` (`publisher_type_owner`/`_agency`/`_developer`). Sem `publisher_type` → cai na heurística 4-signal atual (inalterada).
- [x] AC3: O parser/ingestão de ZAP/VivaReal mapeia o campo nativo do feed → `publisher_type` (normalizar OWNER/AGENCY/DEVELOPER → lower-case enum). MercadoLivre permanece null (segue heurística). → `normalizePublisherType()` + `normalizeListing` (apify.ts) popula `publisher_type`; ML `buildAdvertiserSignals` deixa `undefined`.
- [x] AC4: Telemetria: registrar a fonte da classificação (`deterministic_publisher_type` vs `heuristic_4signal`) em `classification_signals` para auditoria/medição de cobertura. → presença de signal `publisher_type_*` marca a classificação como determinística (vocabulário fechado).
- [x] AC5: Testes em `app/src/lib/scrapers/__tests__/classify-advertiser.test.ts`: cada `publisher_type` → classificação correta + confidence 0.95; ausência de `publisher_type` → heurística preservada (regressão dos casos atuais verde); conflito (publisher_type=owner mas CRECI presente) → publisher_type vence, com signal de conflito registrado.
- [x] AC6: `docs/architecture/adrs/ADR-EPIC7-004` referenciado/anotado: o passo determinístico **precede** a heurística (não a remove — é fallback). → seção "Anotação — Story 7.11" adicionada.

## Dependencies

- Depends on: Story 7.1 (schemas Zod), Story 7.3 (`classifyAdvertiser`).
- Relaciona: ingestão ZAP/VivaReal (Epic 6 busca paramétrica) — fonte do `publisherType`.

## Out of Scope

- Trocar a heurística 4-signal (mantida como fallback p/ fontes sem o campo).
- Enriquecimento de contato do proprietário (Story futura — padrão Captei, com LIA/opt-out).
- Crawler de ZAP/VivaReal em si (já existe via Epic 6).

## Technical Notes

- **Art. IV (No Invention):** o mapeamento OWNER/AGENCY/DEVELOPER vem do schema real dos actors (report F1) — não inventar valores.
- Conflito publisher_type vs CRECI: priorizar `publisher_type` (sinal da plataforma) mas **logar** o conflito (um "OWNER" com CRECI pode ser corretor anunciando como PF — material para auditoria).
- Caveat do report: campo no schema ≠ campo sempre preenchido; tratar null com graça (fallback heurística).

## File List

**Modificados:**
- `app/src/lib/scrapers/classify-advertiser.ts` — `PublisherType`, `publisherType` em `AdvertiserSignals`, Passo 0 determinístico em `classifyAdvertiser`, 4 novos `ClassificationSignal`, helper `normalizePublisherType` (AC2, AC3, AC5).
- `app/src/lib/schemas/epic7/advertisers.ts` — `PublisherTypeSchema`, campo `publisher_type` no `AdvertisersSchema`, 4 sinais novos no `ClassificationSignalSchema` (AC1, AC4).
- `app/src/lib/apify.ts` — `publisher_type` em `NormalizedListing`; `normalizeListing` mapeia `raw.advertiserType` via `normalizePublisherType` (AC3).
- `app/src/lib/scrapers/mercadolivre/toPropertyEpic7.ts` — `publisher_type: null` no envelope ML; passthrough `opts.publisherType` em `buildAdvertiserSignals` (AC3, ML permanece null).
- `docs/architecture/adrs/ADR-EPIC7-004-heuristica-fisbo-deterministica.md` — anotação Story 7.11 (AC6).

**Testes:**
- `app/src/lib/scrapers/__tests__/classify-advertiser.test.ts` — Passo 0 (owner/agency/developer + precedência), conflito owner+CRECI, regressão heurística, `normalizePublisherType` (AC5).
- `app/src/lib/apify.test.ts` — mapeamento `publisher_type` + null para não-canônicos/ausente (AC3).
- `app/src/lib/schemas/epic7/__tests__/advertisers.test.ts` — `PublisherTypeSchema` + sinais novos + campo nullable/optional (AC1).

## Change Log

| Data | Agente | Mudança |
|------|--------|---------|
| 2026-06-15 | @po (Pax) | `*validate-story-draft` → GO 10/10. Status Draft→Ready. Nota p/ @dev: ponto de integração de AC3 é `apify-parametric.ts` (`tipo_anunciante`) + `buildAdvertiserSignals`. |
| 2026-06-15 | @dev (Dex) | Implementação AC1-AC6. Validação: 111/111 testes verdes, 0 erros tsc em `src/`, lint exit 0. Status Ready→InProgress→InReview. |
| 2026-06-15 | @qa (Quinn) | QA Gate: **PASS** (1 obs. low — persistência/wiring fica p/ Story 7.4). 278/278 sem regressão. Status InReview→Done. |

## QA Results

**Gate:** PASS · **Reviewer:** @qa (Quinn) · **Data:** 2026-06-15

```yaml
storyId: 7.11
verdict: PASS
checks:
  code_review: ok        # função pura, vocabulário fechado, Art. IV respeitado
  unit_tests: ok         # 111/111 escopo; AC1-AC6 cobertos
  acceptance_criteria: ok
  no_regressions: ok     # 278/278 em 15 arquivos (scrapers + epic7)
  performance: ok        # Passo 0 O(1) precede I/O CNAE
  security: ok           # sem I/O novo; normalizePublisherType valida entrada
  documentation: ok      # ADR-EPIC7-004 anotado
issues:
  - severity: low
    category: requirements
    description: >
      publisher_type é mapeado no envelope NormalizedListing e consumido por
      classifyAdvertiser, mas ainda não é persistido em scraped_listings nem
      ligado ao upsertListing paramétrico ao vivo.
    recommendation: >
      Acoplar na integração do crawler (Story 7.4) + migration de coluna
      scraped_listings.publisher_type. Fora do escopo declarado da 7.11.
```

**Validação executada (saída real):**
- `vitest run` escopo: 111/111 passed (3 arquivos)
- `vitest run` regressão: 278/278 passed (15 arquivos)
- `tsc --noEmit`: 0 erros em `src/` (único erro é vendored `node_modules/devtools-protocol`, pré-existente)
- `eslint` escopo: exit 0
