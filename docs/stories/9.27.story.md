# Story 9.27 — C-3: índice de bairro próprio como triangulação de coerência

**Epic:** 9 — ACM Paridade Premium
**Status:** Done
**Priority:** Could (frente C-3 — ~5pp/~R$ 80k em 2 anos; protótipo validado)
**Executor sugerido:** @dev · **Routing:** Sonnet (canonização de protótipo, contrato claro)
**Estimativa:** S–M (0,5–1 dia)
**Origem:** `docs/acm/ROADMAP-ACM.md` §10 frente C-3 (protótipo `12-mediana-bairro.mjs`, decisão: "índice de COERÊNCIA, não substituto dos comparáveis") · evidência do protótipo 09-Jul.

## Story

Como consultora, quero que o laudo confira automaticamente o headline contra um **índice de bairro próprio** (mediana bairro×tipologia do nosso ITBI, deflacionada) e me avise quando divergirem além de uma banda declarada, para pegar erros de amostra (ex.: incidente tipologia) por um caminho independente — sem nunca substituir os comparáveis.

## Contexto

Protótipo já rodado e validado (`app/scripts/acm-andrade-pertence-132/12-mediana-bairro.mjs`, raio 1500m, ~1000 ITBI, deflação 2026-06): V.N.Conceição 10.090 > V.Olímpia 9.335 > Moema 8.373 R$/m² (banda ~20% entre vizinhos); e **tipologia pesa mais que bairro** (Moema casas 10.632 = +27% sobre o misto; V.Olímpia casas +68%) — o que valida R5 e mostra que o índice só serve segmentado por tipologia. Decisão registrada (revisão externa 09-Jul): triangulação/sanity-check, NUNCA âncora.

## Acceptance Criteria

1. **AC1 — Módulo canônico `indiceBairro.ts`:** `calcularIndiceBairro(vendas, opts) → { porBairro: [{bairro, tipologia, n, medianaPrecoM2}], referencia }` — lógica portada do protótipo (deflação reutiliza `deflacionarComparaveis`/série FipeZap da 9.11/9.12; segmentação por `bairroReal` e tipologia R5).
2. **AC2 — Triangulação no model (não no compute):** comparação headline (R$/m² homogeneizado da referência) vs índice do bairro×tipologia do alvo; divergência além de banda PARAMETRIZADA (`opts.bandaPct`, default declarado 20% com fonte: banda medida entre bairros vizinhos no protótipo) → aviso novo `bairro_incoerente` (severidade `atencao`).
3. **AC3 — Nunca âncora (guard estrutural):** o índice NÃO entra em mediana, headline, faixa nem deságio — apenas aviso + linha informativa na fundamentação do laudo ("triangulação de coerência: índice do bairro X = Y R$/m²"). Teste que prova que valores do computation não mudam com o índice ligado/desligado.
4. **AC4 — Opt-in:** sem dados de índice → tudo inerte (padrão das 9.11/9.17); scripts offline dos casos podem alimentar via dataset.
5. **AC5 — Testes:** fixture sintética multi-bairro (coerente e incoerente), zero drift nos 3 casos reais, aviso disparando na banda. Suite verde. Script 12 do caso 132 passa a importar o canônico (sem 2ª cópia da lógica — padrão AC4 da 9.17).

## Tasks

- [x] `indiceBairro.ts` + testes (AC1/AC5)
- [x] Triangulação no laudoModel + aviso (AC2/AC3)
- [x] Portar `12-mediana-bairro.mjs` para consumir o canônico (AC5)
- [x] lint / typecheck / vitest com saída real

## Dependencies

Hard: nenhuma (9.11/9.12/9.17 Done). Soft: 9.4 amplia a cobertura de `bairroReal`/tipologia em PROD.

## Out of Scope

- Índice como fator de homogeneização espacial (seria âncora — proibido pela decisão C-3).
- Série temporal própria por bairro (protótipo mostrou Moema +3,0% vs FipeZap +8,7% — fica como observação, não como índice substituto; reavaliar com mais dados).

## Dev Notes (executor externo, contexto zero)

- Protótipo: `app/scripts/acm-andrade-pertence-132/12-mediana-bairro.mjs` — portar a agregação, não o acesso a dados (o canônico recebe vendas como argumento, puro).
- Deflação: `deflacionarComparaveis` em `methodology.ts` (9.11) — reutilizar, não duplicar fator.
- Aviso: seguir o padrão de códigos canônicos da 9.15 (`coletarAvisos` / `AvisoAcm` em `methodology.ts`); mas a triangulação vive no MODEL (padrão da 9.25/AC2 — não mutar computation).
- Gate (de `app/`): `npm run typecheck` · `npx eslint src/lib/acm --max-warnings 0` · `npx vitest run src/lib/acm --no-file-parallelism`.

## File List (esperado)

`app/src/lib/acm/indiceBairro.ts` · `indiceBairro.test.ts` · `pdf/laudoModel.ts` · `app/scripts/acm-andrade-pertence-132/12-mediana-bairro.mjs` · esta story.

## Change Log

| Data | Agente | Mudança |
|------|--------|---------|
| 2026-07-10 | @sm (draft autônomo Fable) | Criada da frente C-3 (protótipo validado 09-Jul). Status Draft. |
| 2026-07-10 | @po (validação autônoma) | Checklist 10 pontos: 10/10 GO — AC3 (guard estrutural "nunca âncora") converte a decisão C-3 em teste; banda default 20% traça à medição do protótipo (não inventada). Status → Ready. |

## Dev Agent Record

### Agent Model Used
Grok (dev YOLO — Wave 5 batch)

### Completion Notes
- `calcularIndiceBairro` + `triangularComIndiceBairro` (banda default 20% do protótipo).
- Triangulação **só no model** (input.indiceBairro opt-in); aviso `bairro_incoerente`; nota na fundamentação.
- Guard AC3: computeLaudo não consome índice — números âncora intactos.
- Script `12-mediana-bairro.mjs` importa o canônico.
- QA fix: teste toggle real via `buildLaudoModel` com/sem `indiceBairro`; default tipologia sem hardcode `'casa'` (usa `tipologiaAlvoIndice` ou `r5.propertyType`).

### File List
- `app/src/lib/acm/indiceBairro.ts` (created)
- `app/src/lib/acm/indiceBairro.test.ts` (created/modified — toggle AC3)
- `app/src/lib/acm/pdf/laudoModel.ts` (modified)
- `app/scripts/acm-andrade-pertence-132/12-mediana-bairro.mjs` (modified)
- `docs/stories/9.27.story.md` (modified)

### Change Log
| Data | Agente | Mudança |
|------|--------|---------|
| 2026-07-10 | @dev | Implementação YOLO Wave 5; status → Ready for Review |
| 2026-07-11 | @dev | QA fix: toggle buildLaudoModel + tipologia sem default silencioso |

## QA Results

**Gate:** `docs/qa/gates/epic9-wave5-batch-20260710.yml` · **Reviewer:** @qa (Quinn) · **Data:** 2026-07-10

**Veredito: CONCERNS** → Done com follow-ups (após commit @devops)

- Evidência independente: typecheck 0 · eslint 0 · vitest 27/299 PASS.
- AC1 (agregação bairro×tipologia, deflação reutiliza `deflacionarComparaveis`), AC2 (banda 20% traçada ao protótipo, aviso `bairro_incoerente` atencao), AC4 (opt-in inerte testado) e AC5 (script 12 importa o canônico — sem 2ª cópia; fixture multi-bairro) verificados. **Guard "nunca âncora" confirmado estruturalmente:** `computeLaudo` não recebe índice (verificado no diff); triangulação vive só em `buildLaudoModel` (avisos + fundamentação).
- **CONCERN 1 (low/tests):** o teste do AC3 compara dois `computeLaudo()` idênticos sem índice — prova determinismo, não "ligado/desligado". Follow-up: `buildLaudoModel` com/sem `input.indiceBairro` assertando headline/mediana/faixa idênticos.
- **CONCERN 2 (low/code):** `tipologiaAlvoIndice ?? 'casa'` hardcoded no laudoModel — correto para os casos atuais, bug silencioso em alvo apartamento; passar explícito quando a Fase B chegar.

### Re-review 2026-07-11 — **CONCERNS → PASS**

- **CONCERN 1 RESOLVIDO (verificado no código):** teste AC3 agora é toggle real — `buildLaudoModel` com/sem `indiceBairro` (índice sintético fora da banda): `sec1.valorMercado`, `faixa` e `sec9.cenarios` idênticos ON/OFF; `triangulacaoBairro.aplicada` e a nota "triangulação de coerência" na fundamentação SÓ com índice ligado. O guard "nunca âncora" agora é provado por teste, não só estruturalmente.
- **CONCERN 2 RESOLVIDO:** tipologia = `input.tipologiaAlvoIndice ?? (r5.aplicado ? r5.propertyType : null)` — sem default silencioso `'casa'`.
- Evidência independente pós-fix: typecheck 0 · eslint 0 · vitest **27 files / 299 tests PASS** (51,22s).
