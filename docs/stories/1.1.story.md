# Story 1.1: Setup do Projeto e Infraestrutura Base

**Epic:** 1 — Foundation, Mapa & Registro de Campo
**Status:** InProgress
**Priority:** Must
**Estimated Complexity:** L

## User Story

Como consultora RE/MAX,
Quero que o sistema esteja configurado e deployado,
Para que eu possa acessar o app via URL no meu celular e começar a trabalhar em campo.

## Acceptance Criteria

- [x] AC1: Projeto Next.js 16+ inicializado com App Router, TypeScript strict, Tailwind CSS e shadcn/ui. Monorepo structure conforme definido na arquitetura. ESLint + Prettier configurados.
- [ ] AC2: Supabase projeto criado com PostgreSQL 16+ e extensões `postgis` e `pgcrypto` habilitadas. Tabelas do núcleo territorial criadas: `consultores`, `epicentros`, `edificios`, `edificios_qualificacoes` com todos os campos do ERD (incluindo `geography(Point, 4326)` para coordenadas). Enums criados: `status_varredura`, `tipologia_edificio`, `padrao_edificio`, `abertura_corretores`, `origem_edificio`.
- [ ] AC3: Supabase Auth configurado com autenticação por email/senha. Row Level Security (RLS) habilitada nas tabelas `edificios_qualificacoes` e `epicentros` com policy `consultant_id = auth.uid()`. Tabela `edificios` com RLS permissiva para leitura (futuramente pública).
- [x] AC4: PWA configurada com `manifest.json` (nome "RE/MAX Moema", tema azul #003DA5, ícone RE/MAX), Service Worker base via Workbox 7.x com `precacheAndRoute` para App Shell (~2 MB). Configuração de `next-pwa` ou equivalente no Next.js.
- [x] AC5: App instalável no celular — meta tags para iOS (`apple-mobile-web-app-capable`) e Android (`display: standalone` no manifest). Testado em Safari iOS e Chrome Android via "Adicionar à tela inicial".
- [ ] AC6: Deploy na Vercel configurado com CI/CD via GitHub Actions. Pipeline: lint → typecheck → test → build → deploy. Branch `main` deploys para produção, PRs geram preview deployments.
- [x] AC7: Tela de login responsiva mobile-first conforme frontend-spec (seção 2.1). Campos email/senha com altura mínima 48px (touch target), botão "Entrar" full-width 56px cor #003DA5, link "Esqueci senha". Shake animation em erro, loading spinner no botão, transição fade para Mapa Principal após login.
- [x] AC8: State management configurado: Zustand para estado global + TanStack Query (React Query) 5.x para server state e cache. Provider configurado no layout root.
- [ ] AC9: Indexes geoespaciais criados: `idx_edificios_coordinates` (GIST), `idx_epicentros_coordinates` (GIST). Indexes de lookup por consultor: `idx_edificios_qual_consultant`, `idx_edificios_qual_edificio`, `idx_edificios_qual_status`.

## Technical Notes

- **Database:** Tabelas `consultores` (id, email, nome, telefone_enc, franquia, regiao_foco, avatar_url, timestamps), `epicentros` (id, consultant_id FK, label, coordinates geography, raio_ativo_m, is_active, timestamps), `edificios` (id, nome, endereco, endereco_normalizado, coordinates geography Point 4326, bairro, cep, cidade, estado, origem enum, seed_source, verificado bool, created_by FK, timestamps), `edificios_qualificacoes` (id, edificio_id FK, consultant_id FK, tipologia enum, padrao enum, status_varredura enum, abertura_corretores enum, oportunidades_count, notas, is_fisbo_detected, timestamps, UNIQUE(edificio_id, consultant_id))
- **API:** Supabase Client SDK para operações CRUD diretas com RLS. Sem API Routes customizadas nesta story.
- **UI Components:** Tela de Login (seção 2.1 do frontend-spec) — Logo RE/MAX centrado 120x80px, campos 48px, botão 56px, shake animation, loading spinner
- **PostGIS:** `CREATE EXTENSION IF NOT EXISTS postgis;` + `CREATE EXTENSION IF NOT EXISTS pgcrypto;`
- **Offline:** Service Worker base (Workbox precache) — apenas App Shell cacheado nesta story. Cache completo na Story 1.6.
- **PWA:** manifest.json, meta tags iOS/Android, Workbox config, ícone RE/MAX 192x192 e 512x512

## Dependencies

- Depends on: Nenhuma (story foundation)
- Blocks: 1.2, 1.3, 1.4, 1.5, 1.6, 1.7 (todas as demais do Epic 1)

## UX Reference

- Screen: Tela de Login (seção 2.1)
- Components: Logo RE/MAX, Input fields (48px), Button primary (56px, #003DA5), Link "Esqueci senha"
- Flow: Login → fade transition → Mapa Principal

## File List

- `app/src/app/layout.tsx` — Root layout com Providers, PWA meta tags, Inter font
- `app/src/app/page.tsx` — Home page com redirect para login se não autenticado
- `app/src/app/(auth)/login/page.tsx` — Tela de login mobile-first RE/MAX
- `app/src/app/globals.css` — Estilos globais + shake animation
- `app/src/lib/supabase/client.ts` — Supabase browser client
- `app/src/lib/supabase/server.ts` — Supabase server client (SSR)
- `app/src/lib/supabase/middleware.ts` — Auth middleware (redirect se não logado)
- `app/src/lib/providers.tsx` — QueryClient + Auth listener
- `app/src/store/auth.ts` — Zustand auth store
- `app/middleware.ts` — Next.js middleware para auth
- `app/public/manifest.json` — PWA manifest RE/MAX
- `app/.env.local.example` — Variáveis de ambiente
- `app/vitest.config.ts` — Configuração Vitest
- `app/src/test/setup.ts` — Test setup com jest-dom
- `app/src/store/auth.test.ts` — Testes do auth store (3 testes)

## Notes

- **Princípio PV — Separação base/qualificação desde o dia 1:** O schema DEVE separar `edificios` (base, futuramente pública) de `edificios_qualificacoes` (privada por consultor). Isso facilita multi-tenant no Epic 4 sem migração.
- **Princípio PV — Funciona sozinho antes de depender de externo:** O setup não deve depender de nenhum serviço externo além de Supabase e Vercel. Mapbox será adicionado na Story 1.2.
- Usar Vitest + Testing Library para testes unitários desde o setup.
- Configurar Sentry para error tracking desde o início.
- Variáveis de ambiente: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` (server only).
