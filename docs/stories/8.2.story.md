# Story 8.2: Camada de cálculo ACM (Score, aderência 50/20/30, efeito-escala, valor residual, sensibilidade)

**Epic:** 8 — Geração de Entregáveis ACM
**Camada:** Cálculo
**Status:** Done (QA gate CONCERNS-approved 2026-06-15 — ver `docs/qa/gates/8.2-acm-camada-calculo.yml`)
**Priority:** Must
**Estimated Complexity:** L (8-13 pts)

> **PO Validation (2026-06-15, Pax):** GO (8/10). Refino: fatores de liquidez são inputs.
> **Implementação (2026-06-15):** `app/src/lib/acm/methodology.ts` (lib pura) + `honduras.fixture.ts` + `methodology.test.ts` — **26/26 testes verdes**, 0 erros de tipo, lint OK. Regressão Honduras confirmada: mediana 18.264, mercado 12.419.520, fechamento 10.217.539, residual 9.624.000, Top 3 = {Chiaffarelli, Bitencourt 101, Torres Homem 399}, deságio -12,7%.
> **AC4 (UI) IMPLEMENTADO (2026-06-15):** painel "Comparáveis mais parecidos" na `AcmScreen` — input de área do alvo → ranqueia por aderência + card de valor indicativo. `app/src/lib/acm/similar.ts` (`selectMostSimilar`, 4 testes) + `app/src/components/acm/AcmSimilarPanel.tsx`. Validado ao vivo com a Luciana (113m² Rua Alvorada → ~R$ 1,25M). Programa (dorms/suítes/vagas) ainda usa área como proxy até o sink ser atualizado (opção B).

## User Story

Como sistema ACM,
Quero uma biblioteca de cálculo pura que reproduza a metodologia do laudo Honduras,
Para que o valor de mercado, a faixa de fechamento e o Top N sejam derivados deterministicamente dos comparáveis — e não apenas média/mediana simples como hoje.

## Contexto

`calculateAcmStats` (em `useAcm.ts` L230) só faz média, mediana e contagens; `tendencia` é stub. O `MATERIAL_DIDATICO` e o `LAUDO` definem a metodologia completa a operacionalizar.

## Acceptance Criteria

- [x] AC1: Lib pura `app/src/lib/acm/methodology.ts` (sem deps Next.js — testável isolada), funções:
  - `classifyScore(precoM2Construido, suites?, vagas?, areaConstruida?)` → `'AAA'|'AA'|'A'|'B'` pela régua do didático (AAA ≥ R$40k + 4+ suítes/vagas ou área ≥500m²; AA ≥30k + 3+; A 22–25k; B <22k).
  - `adherenceIndex(target, comp)` = `0.50·simÁreaConstr + 0.20·simÁreaTerreno + 0.30·proximidade`, cada termo normalizado 0–1 conforme didático Parte 1.3.
  - `landPriceByLotSize(comparaveis)` → R$/m² de terreno mediano por faixa (<500 / 500–800 / >800 m²) — efeito-escala.
  - `residualLandValue(params)` → valor residual do incorporador (VGV − obra − demolição − comercialização − custo financeiro − margem), conforme Laudo Sec. 8b.
  - `sensitivityScenarios(comparaveis, target)` → 3 recortes (Todos / Top 5 / Top 3) com valor de mercado e de fechamento.
  - `liquidityAdjustment(valorMercado, fatores)` → aplica fatores incrementais (exposição, regularização, Capex, liquidez) → valor de fechamento. **Os fatores são inputs por imóvel definidos pela consultora** (ex.: -7/-5/-3/-4 no caso Honduras, Laudo Sec. 2), não constantes do código.
- [x] AC2: `rankByAdherence(target, comparaveis)` retorna a lista ordenada com o índice — base do Top 5/Top 3.
- [x] AC3: Resultado tipado `AcmLaudoComputation` (valor de mercado, faixa de fechamento, Top N, R$/m² mediano, efeito-escala, co-âncora de terreno, deságio medido a partir de `preco_pedido`/`desagio_percent`).
- [~] AC4: `AcmSummaryCards` e `AcmTable` passam a exibir Score e índice de aderência (substituir/estender cards atuais).
  - **PARCIAL (QA CONCERNS):** índice de aderência exposto via painel dedicado `AcmSimilarPanel` ("mais parecidos" + valor indicativo), validado ao vivo com a Luciana. **Score NÃO exibido na UI** — deferido até o sink trazer programa (suítes/vagas/dorms) → 8.1 AC3, pois hoje rodaria sobre proxy de área (baixa confiança). @po reconciliar texto vs realização.
- [x] AC5: **Testes de regressão travando o caso Honduras** (Roadmap Passo 3.3): com os 23 comparáveis do laudo, reproduzir mediana ≈ R$ 18.264/m², valor de mercado ≈ R$ 12,4M, faixa de fechamento R$ 10,0–10,5M, Top 3 = {Chiaffarelli 86, Bittencourt 101, Torres Homem 399}. Tolerância documentada.
- [x] AC6: `calculateAcmStats` legado mantido ou migrado sem quebrar `AcmExportMenu`/`AcmScreen.test.tsx`.

## Dependencies

- Depends on: Story 8.1 (campos construído/terreno, Score, ask-vs-close).
- Blocks: 8.3 e 8.4 (renderização consome `AcmLaudoComputation`).

## Out of Scope

- Regressão hedônica / shrinkage / grau NBR (Roadmap Fase 2 — épico futuro).
- Geração de PDF.

## Technical Notes

- **Não inventar pesos:** 50/20/30, réguas de Score e faixas de lote vêm verbatim do `MATERIAL_DIDATICO_ACM_Honduras.pdf` (Art. IV — No Invention). Cada constante referencia a seção do PDF em comentário.
- Manter a lib determinística e pura para permitir reuso pelo engine e pela edge function.
- Fixture de teste: extrair as 23 vendas do Laudo Sec. 5 para `__tests__/fixtures/honduras.json`.

## File List

**Novos:**
- `app/src/lib/acm/methodology.ts` — lib pura de cálculo (Score, aderência 50/20/30, efeito-escala, valor residual, sensibilidade, liquidez composta, orquestrador `computeLaudo`).
- `app/src/lib/acm/methodology.test.ts` — regressão Honduras + unidades.
- `app/src/lib/acm/honduras.fixture.ts` — 23 comparáveis do Laudo Sec. 5.
- `app/src/lib/acm/adapter.ts` — `fn_comparaveis_no_raio` row → `AcmComparable` (tolerante a schema).
- `app/src/lib/acm/adapter.test.ts`
- `app/src/lib/acm/similar.ts` — `selectMostSimilar` (AC4, reusa aderência).
- `app/src/lib/acm/similar.test.ts`
- `app/src/components/acm/AcmSimilarPanel.tsx` — painel "Comparáveis mais parecidos" + valor indicativo (AC4).

**Modificados:**
- `app/src/components/acm/AcmScreen.tsx` — wiring do `AcmSimilarPanel` (sob guarda de loading/contagem).

**Commits:** `944d466` (lib + regressão) · `1a5e623` (adapter) · `df75872` (AC4 painel) — já em `origin/fix/epic7-v-crawl-health`.

## QA Results

**Gate:** `docs/qa/gates/8.2-acm-camada-calculo.yml` — **Verdict: CONCERNS** (@qa Quinn, 2026-06-15).

- **PASS:** AC1, AC2, AC3, AC5, AC6. Lib pura/determinística, regressão Honduras travada (mediana 18.264, mercado 12.419.520, faixa 10,0–12,5M, Top 3 confirmado, deságio -12,7%). Sem regressão no legado.
- **CONCERNS / PARTIAL — AC4:** índice de aderência exposto em painel dedicado (não em SummaryCards/Table); **Score ausente da UI**, deferido até o sink entregar programa (8.1 AC3). Desvio deliberado e validado ao vivo com a end-user — não bloqueia merge.
- **Testes:** 34/34 lib ACM + 10/10 componentes · 0 erros tsc (src/) · 0 lint.
- **Low (técnico):** cast duplo `as unknown as AcmRpcRow[]` em `AcmSimilarPanel` — limpar após regen de tipos (8.1 AC6).
- **Status autorizado:** InReview → Done (código já commitado e no remoto).
