# Story 9.23 — UI in-app: ativar guard-rails, homogeneização e faixa H-3 nos export sheets ACM

**Epic:** 9 — ACM Paridade Premium
**Status:** Done
**Priority:** Must (Classe S — a UI emite laudo no modelo pré-v5: sem guard-rail, sem deflação, valor-ponto)
**Executor sugerido:** @dev
**Estimativa:** M (1–2 dias)
**Origem:** gap declarado desde H-2 (`docs/acm/ROADMAP-ACM.md` H-2 "UI in-app ainda não expõe homogeneização/guard-rails" + §9b Wave 4) · decisões H-3 (`docs/acm/DECISOES-H3-LUCIANA-20260710.md`)

## Story

Como consultora, quero que o laudo/resumo/deck/didático gerado **pelo app** (export sheets) use os mesmos mecanismos v5 dos laudos offline — guard-rail anti-auto-referência, homogeneização FipeZap, deságio H-3 por estado A–F e headline em faixa — para que o PDF que eu gero em campo tenha a mesma defensabilidade do que o founder gera offline.

## Contexto (problema exato)

Cinco componentes chamam `computeLaudo` no client e TODOS passam o target mínimo, deixando os mecanismos v5 inertes:

| Componente | Chamada | O que falta |
|---|---|---|
| `app/src/components/acm/LaudoExportSheet.tsx` | linha ~122 | `target: { areaConstruida, areaTerreno }` apenas |
| `app/src/components/acm/PacoteExportSheet.tsx` | ~108 | idem |
| `app/src/components/acm/ResumoExportSheet.tsx` | ~107 | idem |
| `app/src/components/acm/EntregavelExportSheet.tsx` | ~118 | idem |
| `app/src/components/acm/PlanilhaExportSheet.tsx` | ~56 | idem |

O irônico: **o formulário já coleta** `endereco`, `vagas`, `pretendido`, `pedidoReal` (ver `LaudoExportSheet.tsx` ~148–167, usados só no `LaudoInput` de exibição) — os dados existem na tela e não chegam ao cálculo. Consequências concretas:

1. Guard-rail 9.8 inerte (sem `target.endereco/vagas/precoPretendido`) — o incidente Honduras 639 pode se repetir no in-app.
2. Sem `homogeneizacao` — comparáveis de 2023 entram a valor nominal (offline deflaciona via FipeZap, Stories 9.11/9.12).
3. Sem `estadoConservacao` — deságio H-3 (régua A–F, Story 9.14 + `ESTADO_DESAGIO_H3`) nunca aplica.
4. Sem `propertyType` — gate R5 (Story 9.17) inerte.
5. `avisos[]` (Story 9.15) e `autoReferenciasExcluidas` são computados mas **nunca mostrados** ao usuário antes do download.

## Acceptance Criteria

1. **AC1 — Guard-rail ativo:** os 5 sheets passam `target.endereco` (campo já coletado), `vagas` e `precoPretendido` ao `computeLaudo`. Quando houver `autoReferenciasExcluidas`, a sheet exibe contagem + endereços + motivos ANTES do botão de download (não bloqueia — informa).
2. **AC2 — Homogeneização por padrão:** `homogeneizacao` ligada com a série vendada (`FIPEZAP_SP_VENDA_RESIDENCIAL` / `FIPEZAP_SP_ULTIMA_COMPETENCIA` de `@/lib/acm/data/fipezapSpVendaResidencial`), exibindo a competência de referência na sheet; toggle explícito para desligar (default ON). Comparáveis sem `dataVenda` seguem o comportamento da 9.11 (sem ajuste, contados em `semAjuste`).
3. **AC3 — Ficha de estado A–F (H-3):** select opcional "Estado do imóvel (régua A–F)" no formulário → `target.estadoConservacao`; ausente → comportamento 9.14 (aviso `target_condition_unconfirmed`, faixa conservadora). Labels da régua conforme `DECISOES-H3-LUCIANA-20260710.md` (A 0% · B −5% · C −7,5% · D −10% · E −15% · F fora da régua).
4. **AC4 — Tipologia do alvo (R5 opt-in):** select opcional casa/apartamento → `propertyType`; documentar na sheet que em PROD a classificação dos comparáveis é parcial até a Story 9.4 (o gate 9.17 já emite `typology_r5_incomplete` nesse caso — só exibir).
5. **AC5 — Transparência pré-download:** a sheet exibe, após o cálculo e antes do download: headline em faixa no formato H-3 ("Mercado R$ X–Y (referência Z)") e a lista de `avisos[]` (ícone por severidade). Aplica-se no mínimo a Laudo, Resumo e Pacote.
6. **AC6 — Zero recálculo e consistência:** nenhum número novo inventado na UI — tudo vem do `AcmLaudoComputation` (ADR-EPIC8-001). Snapshot dos modelos inalterado quando os novos campos não são preenchidos (backward-compat: formulário vazio ≡ comportamento atual).
7. **AC7 — Testes:** testes de componente para (a) endereço preenchido → auto-referência sintética aparece na UI; (b) toggle homogeneização OFF → `homogeneizacao.aplicada === false`; (c) estado C selecionado → deságio −7,5% no computation; (d) formulário mínimo → paridade com snapshot atual. Suite `vitest run src/lib/acm src/components/acm` verde.

## Tasks

- [x] Extrair helper compartilhado (`buildComputeOptions(form)` em `app/src/components/acm/computeOptions.ts`) — os 5 sheets duplicam a montagem hoje; não criar 5 cópias da fiação nova (KISS)
- [x] Fiação AC1 (endereco/vagas/pretendido) nos 5 sheets
- [x] AC2 homogeneização + toggle + exibição da competência
- [x] AC3 select A–F · AC4 select tipologia
- [x] AC5 painel headline faixa + avisos (componente reutilizável `AcmAvisosPanel`)
- [x] AC7 testes de componente + regressão completa
- [x] lint / typecheck / vitest com saída real no Dev Agent Record

## Dependencies

- **Hard:** nenhuma (todos os mecanismos já existem na lib — Stories 9.8/9.11/9.12/9.14/9.15/9.17 Done; série FipeZap vendada no repo).
- **Soft:** 9.4 melhora o R5 em PROD (AC4 já prevê a limitação); 9.22 melhora o guard-rail para o formato de endereço do banco (as stories são independentes e compõem).

## Out of Scope

- Persistir os novos campos do formulário no banco (estado A–F, tipologia) — só estado local da sheet nesta story.
- Alterar modelos/PDFs (laudo/resumo/deck/didático já são v5+H-3+H-4 — Stories 9.13/9.14, commits `bdf3467`/`ef85c49`).
- Fase B / re-verificação web (D-2), ofertas ativas.

## Dev Notes (para o executor — sessão nova, contexto zero)

- **Padrão a seguir:** os scripts offline são a referência de fiação completa — ver `app/scripts/acm-andrade-pertence-132/05-build-laudo.tsx` (~linhas 144–164: target completo + tese + propertyType + homogeneizacao). A UI deve convergir para o mesmo shape de opções.
- **Adapter:** `toAcmComparables` em `app/src/lib/acm/adapter.ts` converte linhas da RPC — verificar se propaga `dataVenda`/`bairroReal` (necessários para AC2; a RPC de PROD expõe só ano em alguns campos — ver memória do caso 113: join por id para competência; se `dataVenda` não chegar ao client, AC2 degrada com `semAjuste` alto e o aviso da 9.11 cobre — comportamento aceitável, documentar).
- **Formato H-3 do headline para AC5:** espelhar `laudoModel.ts` ~596–620 (`mercadoFaixa`/`referenciaNota`) — não reimplementar a lógica; extrair/reutilizar.
- **Testes de componente existentes:** `app/src/components/acm/AcmScreen.test.tsx` mostra o padrão (Testing Library + vitest). Rodar com `npx vitest run src/components/acm --no-file-parallelism`.
- **Gate de qualidade (Art. V):** de `app/`: `npm run typecheck` · `npx eslint src/lib/acm src/components/acm --max-warnings 0` · `npx vitest run src/lib/acm src/components/acm --no-file-parallelism`. Suite lib de referência: 23 files / 271 tests.
- **Armadilhas:** (1) `computeLaudo` roda no client — a série FipeZap é um array estático pequeno, sem custo de bundle relevante; (2) NÃO ligar `tese`/`cenarioDesagio` sem input do usuário (Art. IV — sem default inventado); (3) `estadoConservacao` F = "fora da régua" → sem deságio automático (ver `montarDesagio` em `laudoModel.ts` ~556–571).

## File List

Novos:
- `app/src/components/acm/computeOptions.ts` — helper compartilhado `buildComputeOptions` + opções A–F/tipologia + label FipeZap
- `app/src/components/acm/AcmAvisosPanel.tsx` — painel de transparência (headline faixa H-3 + avisos + auto-referências)
- `app/src/components/acm/computeOptions.test.ts` — 9 testes (AC1/AC2/AC3/AC4/AC6)
- `app/src/components/acm/AcmAvisosPanel.test.tsx` — 5 testes (AC5/AC1)

Modificados (fiação v5):
- `app/src/components/acm/LaudoExportSheet.tsx`
- `app/src/components/acm/PacoteExportSheet.tsx`
- `app/src/components/acm/ResumoExportSheet.tsx`
- `app/src/components/acm/EntregavelExportSheet.tsx`
- `app/src/components/acm/PlanilhaExportSheet.tsx` (AC1–AC4; sem painel — gera XLSX)
- `docs/stories/9.23.story.md`

## Change Log

| Data | Agente | Mudança |
|------|--------|---------|
| 2026-07-10 | @sm (draft autônomo) | Criada a partir do gap declarado desde H-2 + ROADMAP §9b. Status Draft. |
| 2026-07-10 | @po (validação autônoma) | Checklist 10 pontos: 10/10 GO (todos os itens ✓; fatos verificados contra os 5 export sheets — chamadas `computeLaudo` nas linhas citadas — e `LaudoExportSheet.tsx` 95–184). AC6 garante backward-compat testável. Status → Ready. Routing sugerido: Opus (multi-arquivo UI + julgamento de UX; alternativa Sonnet com AC7 rigoroso). |
| 2026-07-10 | @dev (Dex) | Implementados AC1–AC7. Helper compartilhado `computeOptions.ts` + painel `AcmAvisosPanel.tsx`; fiação v5 nos 5 sheets. Gate verde (tsc + eslint 0 warn + 27 files/301 tests). Status → InReview. |
| 2026-07-10 | @qa (Quinn — gate batch Wave 4) | QA gate PASS. Revisor independente rodou tsc (0), eslint acm (0) e `vitest run src/lib/acm src/components/acm --no-file-parallelism` (27 files / 301 tests PASS). AC1–AC7 verificados contra o diff; AC6 backward-compat confirmado (teste de paridade formulário mínimo ≡ target puro); Honduras intacta; File List coerente com o diff. Gate: `docs/qa/gates/epic9-wave4-batch-20260710.yml`. Status → Done. |

## Dev Agent Record

### Decisões de design

- **Helper compartilhado (KISS):** `app/src/components/acm/computeOptions.ts` expõe `buildComputeOptions(form)` que devolve o slice `{ target, homogeneizacao?, propertyType? }` de `ComputeLaudoOptions`. Os 5 sheets espalham (`...`) esse slice sobre os campos próprios (`comparaveis`, `fatoresLiquidez`, `raio`, `residual`). Zero cópias divergentes da fiação. Também centraliza os rótulos da régua A–F (`ESTADO_CONSERVACAO_OPCOES`) e da tipologia (`TIPOLOGIA_OPCOES`) e o label de competência FipeZap.
- **Art. IV (No Invention) no helper:** campos ausentes NÃO viram default — `endereco/vagas/precoPretendido/estadoConservacao/propertyType` só entram no target/opts quando presentes (spread condicional). `tese`/`cenarioDesagio` nunca são inferidos. Estado ausente segue 9.14 (faixa conservadora). Isso garante AC6: formulário mínimo ≡ target puro (teste de paridade explícito).
- **Painel AC5 (`AcmAvisosPanel.tsx`):** componente reutilizável que consome só o `AcmLaudoComputation` (zero recálculo — AC6/ADR-EPIC8-001). Renderiza o headline H-3 "Mercado R$ X–Y (referência Z)" (colapsa em ponto único quando `mercado.min === mercado.max`), a lista de `avisos[]` com ícone por severidade (info/atencao/critico), e o bloco de `autoReferenciasExcluidas` (contagem + endereços + motivos) — informa, não bloqueia (AC1). Aplicado em Laudo, Resumo, Pacote e Entregável (Planilha gera XLSX; recebeu a fiação AC1–AC4 mas sem painel).
- **Prévia recomputada a cada render:** cada sheet chama `buildComputation` para uma `preview` derivada do estado atual do formulário — a transparência atualiza ao vivo conforme a consultora ajusta campos, sem estado de cache que possa divergir do PDF final (o `handleGerar` reusa o mesmo `buildComputation`). Custo desprezível (série FipeZap é array estático pequeno; cálculo puro TS no client).
- **`TipologiaTipo` import:** `methodology.ts` importa `TipologiaTipo` mas não o re-exporta — importado direto de `@/lib/acm/tipologia` (methodology.ts não pode ser editado nesta story). `PlanilhaPropertyType` ('casa'|'apartamento') é subconjunto de `TipologiaTipo`, compatível no `propertyType`.

### Achado do adapter (AC2 — dataVenda/bairroReal)

`toAcmComparable` (`app/src/lib/acm/adapter.ts`) e o tipo `ComparavelNoRaio` (`app/src/lib/supabase/types.ts` 332–366) **NÃO expõem `data_venda`/`bairro`** — a RPC `fn_comparaveis_no_raio` traz `ano_referencia` (só o ano) mas nenhuma competência `YYYY-MM` nem bairro real. Consequência (documentada, Art. IV): em PROD todos os comparáveis do in-app caem em `homogeneizacao.semAjuste` (a deflação exige `dataVenda`), exatamente o degradê previsto pela Story 9.11 — a homogeneização fica ligada e correta, apenas sem pontos para deflacionar até o sink ITBI mapear a competência (Story 9.4). Não inventei dado: o helper liga o mecanismo; o dado que falta é da ingestão. Follow-up abaixo.

### Gate de qualidade (saída real)

```
npm run typecheck        → tsc --noEmit: 0 erros
npx eslint src/lib/acm src/components/acm --max-warnings 0  → 0 warnings / 0 errors
npx vitest run src/lib/acm src/components/acm --no-file-parallelism
                         → 27 files / 301 tests, 301 passed (0 fail)
```

Baseline lib (23 files / 271 tests) permanece 100% verde; +2 arquivos de teste novos (`computeOptions.test.ts` 9 testes, `AcmAvisosPanel.test.tsx` 5 testes) cobrindo AC1 (guard-rail dispara), AC2 (toggle ON/OFF + semAjuste 9.11), AC3 (estado C → −7,5%; ausente → sem deságio), AC4 (R5 aplicado), AC5 (headline faixa + ponto único + bloco de auto-referência), AC6 (paridade formulário mínimo ≡ target puro).

## QA Results

**Data:** 2026-07-10 · **Reviewer:** @qa (Quinn) — revisor independente (não implementou) · **Gate:** `docs/qa/gates/epic9-wave4-batch-20260710.yml`

**Verdict: PASS** → Status Done.

### Evidência (números rodados pelo @qa, de `app/`)

```
npm run typecheck                                              → exit 0 (tsc --noEmit, 0 erros)
npx eslint src/lib/acm src/components/acm --max-warnings 0     → exit 0 (0 warnings/errors)
npx vitest run src/lib/acm src/components/acm --no-file-parallelism
                                                              → 27 files / 301 tests PASS (52.51s)
```

### 7 checks

1. **Code review** — PASS. Helper `buildComputeOptions` monta o slice v5 com spread condicional (Art. IV — campos ausentes não viram default). `AcmAvisosPanel` consome só o `AcmLaudoComputation` (zero recálculo — AC6/ADR-EPIC8-001). KISS respeitado: 1 helper compartilhado pelos 5 sheets em vez de 5 cópias.
2. **Testes** — PASS. 9 testes em `computeOptions.test.ts` + 5 em `AcmAvisosPanel.test.tsx`. Rodados pelo revisor, verdes.
3. **ACs** — PASS. AC1 (guard-rail wired nos 5 sheets + painel de auto-refs antes do download) ✓ · AC2 (homogeneização default ON + toggle + `semAjuste` 9.11) ✓ · AC3 (select A–F → estado C = −7,5% confirmado contra `ESTADO_DESAGIO_H3`) ✓ · AC4 (tipologia R5 opt-in) ✓ · AC5 (headline faixa H-3 + avisos por severidade) ✓ · **AC6 (backward-compat: teste de paridade "formulário mínimo ≡ target puro" presente e verde)** ✓ · AC7 (suite verde) ✓.
4. **Regressões** — PASS. Honduras fixture não tocada; baseline lib (271 tests) permanece verde dentro dos 301.
5. **Performance** — PASS. Série FipeZap é array estático pequeno; `preview` recomputa cálculo puro TS no client — custo desprezível. Nada anômalo.
6. **Segurança** — PASS. Sem segredos/eval/PII nova.
7. **Documentação** — PASS. JSDoc nos novos arquivos; achado do adapter (RPC PROD não expõe `data_venda`/`bairro` → homogeneização degrada p/ `semAjuste` até Story 9.4, coberto pelo aviso 9.11 — Art. IV, não bloqueante) documentado; File List coerente com o diff.

Nenhuma issue medium/high/critical. Follow-up não bloqueante: Story 9.4 habilita deflação real da homogeneização in-app.
