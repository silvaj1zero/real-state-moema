# Validation Report — Story 7.6

**Title:** Pipeline cron-Supabase + Self-Healing Status Tables
**Validator:** @po (Pax)
**Date:** 2026-05-14
**Verdict:** CONCERNS
**Score:** 8/10

## 10-Point Checklist

| # | Criterio | Status | Notas |
|---|---|---|---|
| 1 | Clear and objective title | OK | Explicito sobre cron-Supabase + self-healing |
| 2 | Complete description (User Story) | OK | Justifica WHY ETL deterministico Wave A (vs LangGraph) per ADR-EPIC7-003 |
| 3 | Testable AC (>=5) | OK | 10 ACs: 5 cron jobs nomeados (AC1), SQL functions especificadas (AC2/AC3), boundary contract documented (AC6), smoke E2E procedure (AC9), v_crawl_health view (AC10) |
| 4 | Scope defined (in/out) | OK | Out of Scope: LangGraph Wave B, Temporal Wave C, ITBI ingest table, GeoSampa logica (Story 3.5 existing) |
| 5 | Dependencies mapped | WARN | Depends 7.2, 7.4, 7.5, pg_cron+pgmq. Issue C-06: pgmq plan availability not validated pre-Sprint. Mitigation in Risk #1 (fallback `epic7_task_queue` table). Should require pre-Sprint check. |
| 6 | Complexity estimate | OK | M (5 pts) — abaixo PoC threshold; story is orchestration glue, not net-new heavy compute |
| 7 | Business value | OK | Alinhado FR-059 (cron-Supabase) + NFR-013 (self-healing) + ADR-EPIC7-003 (cron Wave A; LangGraph Wave B boundary contract) |
| 8 | Risks documented | OK | 5 riscos including pgmq fallback, pg_cron timeout 5s, idempotency webhook, infinite retry loop, boundary contract versioning |
| 9 | Definition of Done | OK | 6 arquivos File List + smoke E2E procedure + dashboard view |
| 10 | Alignment PRD/Epic | OK | Rastreia FR-059, NFR-010 (observabilidade), NFR-013 (self-healing) + ADR-EPIC7-003 + AppFolio Realm-X precedent. Article IV satisfied. |

## Cross-Story Implications

### Issue C-01 (Medium) — DUPLICATE cron schedule with 7.4 AC8
- 7.6 AC1 `epic7_mercadolivre_daily` cron schedule (migration `012`).
- 7.4 AC8 ALSO declares pg_cron schedule (migration `010`).
- **Same root issue as 7.4 validation.** One owner per schedule.

### Issue C-06 (Medium) — pgmq availability
- AC5 enables pgmq via `CREATE EXTENSION IF NOT EXISTS pgmq;`. Plan check is conditional ("validar disponibilidade no plano Supabase atual"). If pgmq absent, fallback `epic7_task_queue` manual table.
- **Required:** @data-engineer pre-validates plan supports pgmq BEFORE 7.6 implementation starts. Recommend adding AC11 or Dev Note.

### Issue C-04 (Low) — `fn_mark_stale_runs()` defined in BOTH 7.2 AC5 and 7.6 AC2
- 7.2 declares the function as part of migration 009 (telemetry).
- 7.6 AC2 ALSO declares it as part of migration 013 (self-healing).
- Reading carefully: 7.2 AC5 says "Funcao `fn_mark_stale_runs()` em SQL ... Schedule via pg_cron a cada 5min"; 7.6 AC2 says "Funcao `fn_mark_stale_runs()` (SQL) implementada per ADR-EPIC7-003".
- **Verdict:** Same function named twice, declared once but referenced in both stories. Migration ownership unclear: which migration creates the function?
- **Cleanest:** Function lives in `013_epic7_self_healing_functions.sql` (per 7.6 File List). 7.2 AC5 should be re-worded to say "Function defined in Story 7.6 migration 013; this story relies on it but does not create it."

### Other cross-effects (acceptable)
- AC4 ALTER `crawl_runs` ADD retry_count — explicitly noted "Story 7.2 nao incluiu". Acceptable staged migration, but better practice would be to add at original migration 009. Minor.

## Required Fixes (CONCERNS resolution)

1. **Resolve C-01 (cron schedule ownership):** Coordinate with @sm fix on 7.4 — pick one story to own MercadoLivre cron schedule. Recommend 7.6 owns ALL schedules (single migration `012_epic7_cron_schedules.sql`); 7.4 only creates Edge Functions.
2. **Resolve fn_mark_stale_runs() declaration ownership:** Either 7.2 OR 7.6 owns the function definition. Recommend 7.6 (matches File List `013_epic7_self_healing_functions.sql`). 7.2 AC5 reworded to "consumes fn_mark_stale_runs() defined in Story 7.6".
3. **Add explicit pre-Sprint @data-engineer check for pgmq availability** as Dev Note or AC11.

## Recommended Improvements (optional)
- AC9 smoke test "simular 20min stall artificially" — clarify mechanism (UPDATE crawl_runs SET started_at = NOW() - INTERVAL '20 minutes' WHERE id = test_run_id). Could be Dev Note instead of AC.
- AC10 `v_crawl_health` view — add SECURITY DEFINER or explicit RLS to read scraped_listings.apify_cost_usd (cross-table view). Compliance-by-design.

## Recommendation

**CONCERNS → After @sm coordinates with 7.4 author on cron ownership AND clarifies function ownership AND adds pgmq pre-check, becomes PASS.**

Three fixes are all coordination/wording, not net-new content. Estimated 10-min @sm work. After fixes, status moves to `Ready`.

---
*Pax (@po) — 2026-05-14*
