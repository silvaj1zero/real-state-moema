# Validation Report — Story 7.4

**Title:** Crawler MercadoLivre Imoveis Zona Sul SP
**Validator:** @po (Pax)
**Date:** 2026-05-14
**Verdict:** CONCERNS
**Score:** 8/10

## 10-Point Checklist

| # | Criterio | Status | Notas |
|---|---|---|---|
| 1 | Clear and objective title | OK | Escopo geografico explicito (Moema/Vila Olimpia/Itaim) |
| 2 | Complete description (User Story) | OK | Persona Luciana, valor incremental Epic 6, fonte adicional FISBO clara |
| 3 | Testable AC (>=5) | OK | 10 ACs com criterios fortes: URLs canonicas (AC2), router pattern (AC3), 85% success rate (AC6), telemetry tied to 7.2 (AC7), PoC 100 URLs (AC9) |
| 4 | Scope defined (in/out) | OK | Out of Scope: outros portais Wave B, Telegram, ML scoring, PII anonimization automatica |
| 5 | Dependencies mapped | WARN | Depends 7.1, 7.2, 7.3, **7.5 soft**. **MISSING:** dependency on 7.10 (LGPD) for deploy gating. Risk table acknowledges (R5 "BLOQUEAR deploy ate Story 7.10 GO") but Dependencies field omits this. Should declare 7.10 as deploy-blocker. |
| 6 | Complexity estimate | OK | L (8 pts) — PoC mandatory (AC9 explicit, output `docs/poc/7.4-mercadolivre-smoke.md`) |
| 7 | Business value | OK | Alinhado FR-035 (MercadoLivre crawler) + metric goal "Leads novos/mes >= 200 Wave A" |
| 8 | Risks documented | OK | 5 riscos including LGPD deploy block + cron overlap Epic 6 |
| 9 | Definition of Done | OK | 15 arquivos File List + PoC output path |
| 10 | Alignment PRD/Epic | OK | Rastreia FR-035, FR-058 (robots.txt), FR-066 (minimizacao), ADR-EPIC7-001/002/003. Article IV satisfied. |

## Cross-Story Implications

### Issue C-01 (Medium) — pg_cron schedule duplication
- 7.4 AC8 creates `trigger_mercadolivre_crawl` Edge Function + pg_cron schedule (migration `010_epic7_mercadolivre_cron.sql`).
- 7.6 AC1 ALSO creates `epic7_mercadolivre_daily` cron schedule (migration `012`).
- **Two migrations writing the same cron schedule will conflict** (duplicate `cron.schedule(...)` calls error or no-op depending on `replace` semantics).
- **Required clarification:** 7.4 should OWN the Edge Function only; 7.6 should OWN the cron schedule. OR add `IF NOT EXISTS`/`cron.unschedule` guard.

### Issue C-04 (Medium) — Calendar coupling to 7.9
- 7.9 workshop requires "100+ anuncios reais em DB" + "14 dias acumular volume". 7.4 must deploy AND accumulate before 7.9 can execute. Sprint planning concern; not a story-content fix.

### Issue C-07 (High, external) — 7.10 LIA blocker
- 7.4 Risk R5 captures: "LGPD: capturar telefone PF sem LIA | BLOQUEAR deploy ate Story 7.10 (LIA) GO". This must be visible in Dependencies field, not only in Risks.

## Required Fixes (CONCERNS resolution)

1. **Add `7.10` to Dependencies field explicitly** with note "DEPLOY GATE: cannot deploy to production until 7.10 Done". Risk R5 captures it but Dependencies should mirror for visibility.
2. **Resolve cron schedule ownership (C-01):** Choose one:
   - **Option A (preferred):** Modify AC8 to remove cron schedule creation; declare "cron schedule managed by Story 7.6 migration 012". Keep Edge Function creation in 7.4.
   - **Option B:** Add `WHERE NOT EXISTS` guard so both migrations are idempotent.
   @sm decides + edits AC8 wording.

These are CONCERNS-level (PROCESS fixes, not core content). 8/10 already meets PASS threshold (>=7), but cleaner to fix before @dev start to avoid mid-implementation rework.

## Recommended Improvements (optional)
- AC5 "intelligence_feed" event — confirm Epic 3 schema exists; add Dev Note "Verify `intelligence_feed` table exists pre-AC5 implementation".
- AC8 webhook security: declare HMAC/secret validation explicitly (currently mentioned as "validar token webhook" in 7.6 AC8). Tighten.

## Recommendation

**CONCERNS → After @sm applies the 2 required fixes, becomes PASS.**

**Decision path:**
- IF @sm applies fixes within Sprint 1 kickoff window → re-validate (1-line update). Then status moves to `Ready`.
- IF @sm does not apply, story remains `Draft` and is escalated to @aiox-master.

The story is OPERATIONALLY ready (content quality 9/10, Article IV satisfied), but the cron-ownership ambiguity is a real risk that could surface as a migration-fails-in-prod bug. Better to clarify in PO phase than discover in QA.

**Recommendation to founder:** the 2 fixes are 5-minute edits. Approve @sm to apply during validation feedback loop, then this validation re-runs PASS.

---
*Pax (@po) — 2026-05-14*
