# Validation Report — Story 7.5

**Title:** Container CNPJ ETL (rictom/cnpj-sqlite) + Tabela `cnpj_enrichment`
**Validator:** @po (Pax)
**Date:** 2026-05-14
**Verdict:** PASS
**Score:** 10/10

## 10-Point Checklist

| # | Criterio | Status | Notas |
|---|---|---|---|
| 1 | Clear and objective title | OK | Container + tabela explicitos; cita upstream OSS rictom/cnpj-sqlite |
| 2 | Complete description (User Story) | OK | Justifica via classifyAdvertiser dependency + latencia target <50ms |
| 3 | Testable AC (>=5) | OK | 10 ACs com numeros/regex/SHA pin specificados: 10 CNAEs verbatim (AC2), DB size <2GB (AC5), <50ms query (AC8), backup S3 12mo retention (AC9) |
| 4 | Scope defined (in/out) | OK | Out of Scope: Socios Brasil (Wave B), real-time CNPJ lookup, encryption granular socios (delegated to 7.10) |
| 5 | Dependencies mapped | OK | Depends 7.2 (crawl_runs telemetry), Supabase Vault/pgcrypto; blocks 7.3, 7.4, Wave B portal crawlers |
| 6 | Complexity estimate | OK | L (8 pts) — PoC mandatory (AC8 explicit, dump real mes anterior, <50ms validation, output `docs/poc/7.5-cnpj-etl-smoke.md`) |
| 7 | Business value | OK | Alinhado FR-041 (CNPJ container) + ADR-EPIC7-005 + metric "filtro CNAE imobiliario SP ~50-100k registros" |
| 8 | Risks documented | OK | 5 riscos com mitigacao: RFB schema change (test mensal AC7), build > 4h (Hetzner fallback), maintainer abandono (SHA pin), PII socios (Story 7.10), volume drag classify (cache 7.3 + index) |
| 9 | Definition of Done | OK | 13 arquivos File List + backup helper restore_cnpj_db.sh + GitHub Actions workflow |
| 10 | Alignment PRD/Epic | OK | Rastreia FR-041, FR-044, NFR-012 (LGPD) + ADR-EPIC7-005 (decisao container Python isolado) + `docs/code-anatomy/rictom-cnpj-sqlite/extraction-notes.md`. CNAE codes VERBATIM do oficial gov.br. Article IV: 100%. |

## Cross-Story Implications

- **Provides `cnpj_enrichment` table for 7.3 `lookupCNAE`.** AC3 RLS notes: socios JSONB NAO expor para `authenticated` baseline (per 7.10 cifragem refinada).
- **AC10 socios PII pre-emptively addressed:** 7.5 acknowledges socios.cpf_cnpj is PII, mitigacao delegated to 7.10. Coherent boundary.
- **AC1 container build environment:** declares either GitHub Actions self-hosted runner OR Hetzner CPX21 dedicated. @devops decides at deploy time. Story acknowledges both paths in Technical Notes.
- **AC7 alert thresholds well-quantified:** ±5% mes-a-mes filtros, build > 4h (2x mediana), share_token change. Concrete monitoring spec for 7.6 healer pipeline to consume.

## Required Fixes
None.

## Recommended Improvements (optional)
- AC4 `push_supabase.sh` uses `psql COPY` OR `supabase-py` — pick one in @dev refinement to avoid ambiguity. Recommendation: `psql COPY` (faster batch upsert, no Python dep).
- AC6 schedule notes "GitHub Actions self-hosted runner OR Hetzner CPX21 dedicado" — @devops decides pre-7.5 start. Add Dev Note for clarity.
- LGPD audit hook: 7.5 inserts records but does NOT log to `lgpd_audit`. 7.10 trigger BEFORE INSERT/UPDATE auto-encrypts; verify trigger covers `cnpj_enrichment.socios JSONB`. Note in 7.10 cross-ref.

## Recommendation

**PASS — promote Draft → Ready.** Article IV satisfied (CNAE codes verbatim from official source, ADR-EPIC7-005 cited, code-anatomy rictom/cnpj-sqlite cited). 8pt PoC is well-defined. @dev Pre-Flight mode strongly recommended (Python runtime + container + SHA pin + monthly scheduling — many decisions need clarification upfront).

---
*Pax (@po) — 2026-05-14*
