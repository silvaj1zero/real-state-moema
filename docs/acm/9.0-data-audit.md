# Story 9.0 — Auditoria de dados ACM (achados)

**Data:** 2026-06-17 · **Executor:** @dev (Dex) · **Tipo:** spike READ-ONLY (nenhuma alteração de schema/dado)
**Fonte:** `app/scripts/acm-audit/9.0-data-audit.mjs` (PostgREST + SERVICE_ROLE_KEY contra PROD `hculsnvpyccnekfyficu`)
**Consultor ITBI:** `1f7ec2b3-d414-4850-8b6a-32faa8e1f47c` (Luciana) — **3.618 linhas ITBI**.

> Nota de método: o PostgREST capa em **1000 linhas/request**. As médias por amostra foram **confirmadas por contagem exata** (`count: exact, head: true`) sobre as 3.618 linhas. Onde houver número, é exato.

---

## AC1 — Contrato da RPC `fn_comparaveis_no_raio` (VIVA em PROD)

**Resultado:** a RPC viva retorna **23 colunas**, incluindo `latitude`, `longitude` e `anuncio_url`:

```
comparavel_id, endereco, area_m2, preco, preco_m2, is_venda_real, fonte, distancia_m,
area_construida_m2, area_terreno_m2, preco_m2_terreno, dormitorios, suites, vagas,
score, sql_cadastral, ano_referencia, preco_pedido, desagio_percent, status_anuncio,
latitude, longitude, anuncio_url
```

- **A migration `20260616000001` (Story 8.7) ESTÁ aplicada em PROD.** A contradição registrada na comparação está **resolvida**.
- Na amostra (Moema, raio 1000 m), **1000/1000 comparáveis com `latitude`/`longitude` não-nulo** (coords = `ST_Y/ST_X` de `coordinates`, que tem 0% NULL — ver AC2).

➡️ **Decisão de escopo:** **Story 9.3 (mapa Top-5 numerado + pins) está DESTRAVADA no dado** — é majoritariamente camada de render. Não depende da 9.4.

---

## AC2 — Cobertura NULL dos campos de metodologia (3.618 linhas ITBI)

Contagem exata sobre as 3.618 linhas:

| Campo | NULL | % |
|---|---:|---:|
| `area_construida_m2` | 3.618 | **100%** |
| `area_terreno_m2` | 3.618 | **100%** |
| `preco_m2_terreno` | 3.618 | **100%** |
| `testada_m` | 3.618 | **100%** |
| `ano_construcao` | 3.618 | **100%** |
| `padrao_iptu` | 3.618 | **100%** |
| `valor_venal` | 3.618 | **100%** |
| `tipo` | 3.618 | **100%** |
| `sql_cadastral` | 3.618 | **100%** |
| `dormitorios` / `suites` / `vagas` | 3.618 | **100%** |
| `score` | 3.618 | **100%** |
| `preco_pedido` / `desagio_percent` | 3.618 | **100%** |
| `status_anuncio` | 3.618 | **100%** |
| `scraped_listing_id` | 3.618 | **100%** |
| `data_referencia` | 0 | **0%** (populado) |
| `coordinates` | 0 | **0%** (populado) |

➡️ **Conclusão:** hoje o ITBI traz apenas **endereço, preço, coordenadas e data**. **Todo o resto da metodologia está vazio** — confirma que o sink (`supabase_acm.py`, AC3 do Epic 8) **ainda não mapeia** os campos. **Story 9.4 é o bloqueio real da paridade de evidência** (score com confiança, rastreabilidade SQL, S/V/D, aba Terrenos).

---

## AC3 — Disponibilidade para distinguir apartamento × casa

- `tipo` = **100% NULL** nas 3.618 linhas → **o banco hoje NÃO distingue apto de casa**.
- Não há, hoje, área privativa / andar / vista (campos inexistentes ou não populados).

➡️ **Decisão de escopo:** a régua de apartamento da **Story 9.1 depende de 9.4** popular `tipo` (e, idealmente, área privativa/programa) a partir da fonte ITBI/IPTU. Até lá, o discriminador apto/casa só funciona com **entrada manual** do consultor (seletor na UI) — não derivado do dado. Recomendação: 9.1 implementa o discriminador + seletor manual; a classificação automática por tipo fica condicionada à 9.4.

---

## AC4 — Amostra real (1 alvo + raio)

- Centro: geocode real de **"Alameda dos Maracatins, Moema, SP"** (Mapbox) → `-23.608469, -46.662208` (não inventado).
- RPC raio 1000 m → comparáveis reais retornados (amostra, capada em 1000):

```
1. AV JURUCE 335        | 24m  | lat=-23.6084269 lng=-46.6624382 | score=— | sql=—
2. AV JURUCE 387        | 34m  | lat=-23.6087405 lng=-46.6620502 | score=— | sql=—
3. AL DOS JURUPIS 1035  | 111m | lat=-23.6075848 lng=-46.6627178 | score=— | sql=—
```

- Coords presentes e plausíveis (Moema). `score`/`sql` vazios (coerente com AC2).
- **Observação de dado:** há linhas duplicadas de mesmo endereço (AL DOS JURUPIS 1035 ×3) — possível efeito de guias múltiplas / falta de dedup por SQL (a dedup depende de `sql_cadastral`, que está NULL → mais um item para a 9.4).

---

## Impacto no escopo do Epic 9 (recomendações)

| Story | Ajuste pós-spike |
|---|---|
| **9.1** (apto/casa) | Implementar discriminador + **seletor manual de tipo** na UI; classificação automática por `tipo` fica **dependente de 9.4**. Régua de apto segue elicit com a Luciana. |
| **9.2** (XLSX) | Gerar já, mas a maioria das colunas (S/V/D, SQL, terreno, status) virá **vazia** até a 9.4 — degradação graciosa é **obrigatória**, não opcional. Aba **Terrenos** fica vazia até 9.4 (sem área de terreno/fração). |
| **9.3** (mapa) | **DESTRAVADA** — lat/lng vivos. Promover a camada de render (pins Top-5 numerados). Pode subir de prioridade (entrega visível rápida). |
| **9.4** (sink) | **É o gargalo.** Sem ela, 9.1 (auto), 9.2 (colunas), rastreabilidade e dedup por SQL não fecham. Adicionar à 9.4 a **dedup por SQL** (duplicatas observadas) como verificação. |

➡️ **Sequência sugerida revisada:** 9.3 (rápida, destravada) ‖ 9.4 (gargalo, cross-repo) → 9.1 + 9.2 (consomem o dado da 9.4). 

## AC6 — Nenhuma alteração de schema/dado

Confirmado: spike 100% read-only (apenas `SELECT`/RPC e geocode). Nenhum `INSERT/UPDATE/DDL`.
