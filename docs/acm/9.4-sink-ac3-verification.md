# Story 9.4 — Contrato de sink ITBI + verificação PROD (AC3/AC4)

**Data:** 2026-07-09 · **Repo de código do sink:** `acm-imobiliario` (`engine/src/sinks/supabase_acm.py`)  
**Repo de aceite:** este (`real-state-moema`) — validação de cobertura no banco PROD  
**Baseline 9.0:** `docs/acm/9.0-data-audit.md` (3.618 ITBI · **100% NULL** nos campos de metodologia)

> Fronteira (Art. II / story): o `.py` **não** é editado aqui. @data-engineer + @devops alteram o engine; este doc é o **contrato + checklist de aceite**.

---

## 1. Contrato de campos (AC1 + ampliação R5)

### 1.1 Já previstos na migration `20260615000002` (mapear no sink)

| Coluna PROD | Fonte na guia / ITBI | Uso no app |
|-------------|----------------------|------------|
| `area_construida_m2` | Área construída | Score, mediana R$/m², aderência |
| `area_terreno_m2` | Área do terreno | Lente terreno Sec. 8, pesos 9.16 |
| `preco_m2_terreno` | derivado ou fonte | Aba Terrenos / Sec. 8 |
| `sql_cadastral` | id_fonte / cadastro | R5, rastreabilidade, visual override |
| `padrao_iptu` | Descrição do padrão | R5 (HORIZONTAL/VERTICAL) |
| `ano_construcao` / `ano_referencia` | ACC / exercício | Ficha / idade |
| `testada_m` | Testada | Terreno |
| `valor_venal` | Valor venal | Triangulação |
| `tipo` | Uso / tipo | AC2 apto×casa |
| `dormitorios` / `suites` / `vagas` | anúncio casado | Score luxo; NULL p/ ITBI puro OK |

### 1.2 Ampliação R5 (incidente tipologia 09-Jul) — **obrigatório para R5 em PROD**

| Campo (sugerido) | Fonte guia | Por quê |
|------------------|------------|---------|
| `complemento` | Complemento (`AP 82`) | Causa-raiz casa×apto |
| `uso_iptu` | Descrição do uso | R5 `classificarTipologia` |
| `fracao_ideal` | Fração ideal | Terreno real se = 1 |
| (já listados) testada, ACC, áreas | | |

**Consumidor canônico no app:** `app/src/lib/acm/tipologia.ts` (`GuiaTipologiaFields` + `AcmComparable.usoIptu|complemento|sqlCadastral`).

### 1.3 Mapeamento esperado no sink (`montar_registros`)

```
id_fonte / cadastro     → sql_cadastral
área construída         → area_construida_m2  (NÃO colapsar só em area_m2)
área terreno            → area_terreno_m2
complemento             → complemento (nova coluna ou notas estruturadas — preferir coluna)
descrição do uso        → uso_iptu + tipo (casa|apartamento|terreno|…)
descrição do padrão     → padrao_iptu (texto — coluna ALINHADA p/ TEXT na 20260711000001)
ACC                     → ano_construcao
fração ideal            → fracao_ideal
testada                 → testada_m
valor venal             → valor_venal
```

**Idempotência (AC3):** reprocessar o mesmo SQL não duplica linha; `UPDATE` por `(consultant_id, sql_cadastral, data_referencia, preco)` ou chave estável do engine.

---

## 2. Metas de cobertura (AC4) — após backfill

Baseline 9.0 = 100% NULL. Metas mínimas pós-9.4 (ITBI Moema, consultant Luciana):

| Campo | Meta NULL |
|-------|-----------|
| `sql_cadastral` | ≤ 5% |
| `area_construida_m2` | ≤ 5% |
| `area_terreno_m2` | ≤ 40% (muitos aptos sem terreno útil) |
| `tipo` **ou** `uso_iptu` | ≤ 20% |
| `complemento` | best-effort (preencher quando guia tiver) |
| `dormitorios/suites/vagas` | sem meta em ITBI puro |

Linhas **não-ITBI** (manual/scraping): intactas (AC6).

---

## 3. Como verificar neste repo

### 3.1 Script de cobertura (READ-ONLY)

```bash
cd app
npx -y tsx scripts/acm-audit/9.4-sink-coverage.mjs
```

Requer `.env.local` com `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`.  
Imprime tabela NULL% vs baseline 9.0 e exit **1** se metas mínimas de `sql_cadastral` / `area_construida_m2` falharem.

### 3.2 Smoke R5 em PROD (após sink)

Com amostra de SQLs conhecidos (ex.: Andrade 45 = AP 82):

```bash
cd app
npx -y tsx scripts/acm/acm-validate.tsx docs/acm/andrade-pertence-132 --lite
```

(Offline com `dataset.json` já R5-limpo; caminho PROD via RPC fica no follow-up quando 9.4 popular colunas.)

### 3.3 RPC (AC5) — ✅ FEITO 11-Jul (migration `20260711000002`)

`fn_comparaveis_no_raio` estendida em PROD para 32 colunas: contrato 8.1/8.7 preservado verbatim + `padrao_iptu`(text)/`tipo`/`ano_construcao`/`testada_m`/`valor_venal` + R5 (`complemento`/`uso_iptu`/`fracao_ideal`/`bairro_real`) + `data_venda` (:= `data_referencia`, 100% preenchida nas ITBI — homogeneização 9.23 destravada no banco). Smoke em PROD: 193 comparáveis no raio de coordenada real, `data_venda` 193/193. Trap 42P13 respeitada (DROP+CREATE+re-GRANT).

---

## 4. Checklist de handoff @devops / @data-engineer

- [ ] PR no `acm-imobiliario`: mapeamento `montar_registros` (AC1 + §1.2)
- [x] Migration `complemento` / `uso_iptu` / `fracao_ideal` / `bairro_real` + `padrao_iptu`→TEXT — **aplicada em PROD 11-Jul** (`20260711000001`)
- [x] RPC estendida (AC5) — **aplicada em PROD 11-Jul** (`20260711000002`)
- [ ] Backfill idempotente Moema 3.618 (AC3) + log contagem antes/depois
- [ ] Release engine + re-sink
- [ ] Rodar `9.4-sink-coverage.mjs` neste repo → anexar saída neste doc
- [ ] Smoke: RPC raio Moema + 1 laudo Lite
- [ ] Atualizar story 9.4 → Done quando metas §2 OK

---

## 5. Resultado da verificação (preencher pós-backfill)

| Data | Executor | sql_cadastral NULL% | area_construida NULL% | tipo/uso NULL% | Notas |
|------|----------|---------------------:|----------------------:|---------------:|-------|
| 2026-06-17 | 9.0 audit | 100 | 100 | 100 | baseline |
| 2026-07-11 | @data-engineer (sessão B) | 100 | 100 | 100 | **schema pronto** — colunas R5 criadas + RPC 32 cols; NULLs só caem com sink/backfill (sessão C) |
| _pendente pós-backfill_ | | | | | |

---

## 6. Relação com o plano ACM

| Item | Dependência de 9.4 |
|------|-------------------|
| R5 **offline** (scripts + 9.17) | Soft — já opera com guias SF |
| R5 **em PROD em escala** | **Hard** — Complemento/Uso no sink |
| P-1 CLI offline | Soft — datasets JSON |
| P-1 CLI “só endereço” via RPC | Hard — colunas preenchidas |
