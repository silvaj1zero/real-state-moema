type: object
required:
  - id
  - gate
  - blocks_if
  - pass_if
properties:
  id:
    type: string
  gate:
    type: string
  blocks_if:
    type: array
    items:
      type: string
  pass_if:
    type: array
    items:
      type: string
  score_weight:
    type: integer
  absorbed_from:
    type: array
    items:
      type: string

