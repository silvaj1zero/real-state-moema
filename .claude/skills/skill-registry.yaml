registry:
  last_updated: "2026-04-21"
  total_skills: 139
  # sinkra-validate-squad + sinkra-upgrade-squad + full-sdc registered 2026-04-20 — were production-used but unregistered
  # sinkra-map-process registered 2026-04-20 — Tier2 orchestrator for 7-phase SINKRA process mapping
  # cc-session-analyze added 2026-04-18 — wraps session-usage-report.py; ADR-019 (cross-repo parity with aiox-enterprise ADR-015)
  # context-export added 2026-04-21 — inline HTML dashboard of current session working memory; meta-observation tool
  description: "Canonical catalog of all skills in .claude/skills/"

skills:
  - name: sinkra-validate-squad
    version: "2.0.0"
    owner_squad: sinkra-squad
    sinkra_tier: Tier2
    context: conversation
    user-invocable: true
    path: .claude/skills/sinkra-validate-squad/SKILL.md
    description: "3-Tier SINKRA Squad Validation with structural inventory, LLM scoring, and cross-reference investigation. Modes: quick/standard/deep."
    invoked_by: [/sinkra-upgrade-squad]
  - name: sinkra-upgrade-squad
    version: "1.0.0"
    owner_squad: sinkra-squad
    sinkra_tier: Tier2
    context: conversation
    user-invocable: true
    path: .claude/skills/sinkra-upgrade-squad/SKILL.md
    description: "8-phase SINKRA-native squad upgrade (compliant to native). Consumes /sinkra-validate-squad output as input."
    invokes: [/sinkra-validate-squad, /sinkra-map-process]
  - name: full-sdc
    version: "1.0.0"
    owner_squad: sinkra-squad
    sinkra_tier: Tier2
    context: conversation
    user-invocable: true
    path: .claude/skills/full-sdc/SKILL.md
    description: "Full Story Development Cycle — orchestrates validate → develop → review (QG loop) → deploy → close via Agent Teams with sequential handoffs and visual task tracking."
    invokes: [/validate-story-draft, /develop-story, /review-story, /apply-qa-fixes, /deploy-story, /verify-deploy, /close-story]
  - name: sinkra-map-process
    version: "1.1.0"
    owner_squad: sinkra-squad
    sinkra_tier: Tier2
    context: conversation
    user-invocable: true
    path: .claude/skills/sinkra-map-process/SKILL.md
    description: "SINKRA 7-phase process mapping pipeline with checkpoint enforcement (PV_BS_001, PV_PA_001, PV_PM_001, META_AXIOMAS, SINKRA_QA_GATE). Invoked by /sinkra-upgrade-squad Phase 3. Produces handoff-downstream.yaml consumed by /squadCreator:squad-chief."
    invokes: [/squadCreator:squad-chief]
    invoked_by: [/sinkra-upgrade-squad]
  - name: cc-session-analyze
    version: "1.0.0"
    owner_squad: claude-code-mastery
    sinkra_tier: Tier1
    context: inline
    user-invocable: true
    path: .claude/skills/cc-session-analyze/SKILL.md
    description: "Analyze Claude Code session recordings (~/.claude/projects/*/*.jsonl) — thin wrapper over claude-code-mastery/scripts/session-usage-report.py. Replaces speculative telemetry hook per ADR-019."
    adr: ADR-019
  - name: context-export
    version: "1.0.0"
    owner_squad: claude-code-mastery
    sinkra_tier: Tier1
    context: inline
    user-invocable: true
    path: .claude/skills/context-export/SKILL.md
    description: "Generate a visual HTML dashboard of the current session context window — memory layers, token budget, conversation timeline, artifacts, agents invoked, decisions log. Uses AIOX brand tokens (dark + lime + mono). Complementary to cc-session-analyze: this reads the live working memory, cc-session-analyze reads historical JSONL files on disk."
  - name: vault-graph
    version: "1.0.0"
    owner_squad: sinkra-squad
    sinkra_tier: Tier2
    context: inline
    user-invocable: true
    path: .claude/skills/vault-graph/SKILL.md
    description: "9-operation interactive graph analysis for the knowledge vault"
    story: RV-3.5
