# Extracted from: .claude/skills/roundtable/SKILL.md (Presets section + ATM-RT-002)
# Part of: SP-ROUNDTABLE process
# Version: 1.1.0
#
# Schema: presets
#   <preset_id>:                   # string — unique preset identifier (snake_case)
#     agents: string[]             # REQUIRED — ordered list of agent refs (@agent-name) for this preset
#     suggested_mode: string       # REQUIRED — one of: review | decision | gap_analysis | investigation | strategic | audit
#     flags: string[]              # OPTIONAL — CLI-style flags (e.g., "--extended", "--fast") that modify behavior

presets:

  full_technical:
    agents:
      - "@architect"
      - "@cso"
      - "@qa"
      - "@db-sage"
      - "@aiox-master"
    suggested_mode: review

  governance_review:
    agents:
      - "@cso"
      - "@aiox-master"
      - "@sinkra-chief"
    suggested_mode: review

  architecture_review:
    agents:
      - "@architect"
      - "@devops"
      - "@infra-chief"
      - "@cso"
    suggested_mode: review

  story_review:
    agents:
      - "@po"
      - "@qa"
      - "@cso"
    suggested_mode: review

  epic_review:
    agents:
      - "@architect"
      - "@cso"
      - "@qa"
      - "@db-sage"
      - "@aiox-master"
    suggested_mode: review

  final_approval:
    agents:
      - "@cso"
      - "@vision-chief"
      - "@coo"
      - "@cto"
      - "@sinkra-chief"
      - "@squad-chief"
      - "@architect"
      - "@devops"
      - "@infra-chief"
      - "@aiox-master"
    suggested_mode: review
    flags:
      - "--extended"

  architecture_decision:
    agents:
      - "@architect"
      - "@cso"
      - "@devops"
      - "@infra-chief"
      - "@sinkra-chief"
    suggested_mode: decision

  gap_discovery:
    agents:
      - "@squad-chief"
      - "@sinkra-chief"
      - "@cso"
      - "@coo"
      - "@architect"
    suggested_mode: gap_analysis

  state_investigation:
    agents:
      - "@cso"
      - "@architect"
      - "@qa"
      - "@db-sage"
      - "@aiox-master"
      - "@sinkra-chief"
    suggested_mode: investigation

  strategic_planning:
    agents:
      - "@vision-chief"
      - "@cso"
      - "@coo"
      - "@cto"
      - "@pm"
      - "@architect"
    suggested_mode: strategic

  quick_decision:
    agents:
      - "@cso"
      - "@architect"
    suggested_mode: decision
    flags:
      - "--fast"
