# Consolidation Artifact Template — Governance Pipeline
# Extends handoff format (D3: RT-2026-03-28-002)
# Generated by: MOL-GP-001 ATM-GP-002
# Consumed by: MOL-GP-002 ATM-GP-004 (@architect)

meta:
  id: "GP-{{YYYY}}-{{NNN}}"
  type: consolidation  # D3: extends handoff with this type
  template_version: "1.0.0"
  created: "{{YYYY-MM-DD}}"
  consumed: false
  source_type: "{{roundtable | handoff | research | sinkra_output}}"
  source_ref: "{{path to original file}}"
  pipeline_state: CONSOLIDATING

# ─────────────────────────────────────────────
# DECISIONS (extracted from source)
# ─────────────────────────────────────────────
decisions:
  # - id: "D1"
  #   text: "Decision text"
  #   vote: "unanimous | majority | split"
  #   source: "RT-YYYY-MM-DD-NNN"
  #   impact: "architectural | behavioral | scope"

# ─────────────────────────────────────────────
# CONDITIONS (extracted from source)
# ─────────────────────────────────────────────
conditions:
  # - id: "K1"
  #   severity: "HIGH | MEDIUM | LOW"
  #   text: "Condition text"
  #   enforced_at: "phase where this must be checked"

# ─────────────────────────────────────────────
# GAPS (if from gap_analysis or investigation)
# ─────────────────────────────────────────────
gaps:
  # - id: "GAP-1"
  #   description: "Gap description"
  #   severity: "CRITICAL | HIGH | MEDIUM | LOW"
  #   resolution: "proposed resolution"

# ─────────────────────────────────────────────
# NEXT STEPS (actionable items for pipeline)
# ─────────────────────────────────────────────
next_steps:
  # - step: "What needs to happen"
  #   agent: "@architect | @pm | @sm | @po"
  #   priority: "P0 | P1 | P2"

# ─────────────────────────────────────────────
# PARTICIPANTS / STAKEHOLDERS
# ─────────────────────────────────────────────
participants:
  # - name: "Name"
  #   role: "Accountable | Consulted | Informed"

# ─────────────────────────────────────────────
# ARTIFACT CHAIN (grows as pipeline progresses)
# ─────────────────────────────────────────────
artifact_chain:
  # - ref: "path/to/source"
  #   type: "roundtable | consolidation | adr | epic | story | gap_analysis | pipeline_report"
  #   phase: "MOL-GP-001"
  #   timestamp: "YYYY-MM-DDTHH:MM:SS"

# ─────────────────────────────────────────────
# READINESS (ATM-GP-003 populates)
# ─────────────────────────────────────────────
readiness:
  has_decisions: false
  has_next_steps: false
  no_blocking_vetos: false
  has_participants: false
  ready: false
